# 智能优化算法学习笔记

> 本文档汇总了基于本地项目 PDF 的论文总结，以及对蚁群算法（ACO）和群体增量学习算法（PBIL）的详细讲解。

---

## 目录

1. [论文一：改进蚁群优化算法求解折扣 {0-1} 背包问题](#论文一改进蚁群优化算法求解折扣-0-1-背包问题)
2. [论文二：混合群体增量学习算法求解闭环布局问题](#论文二混合群体增量学习算法求解闭环布局问题)
3. [蚁群算法（ACO）详解](#蚁群算法aco详解)
4. [PBIL 算法详解](#pbil-算法详解)

---

## 论文一：改进蚁群优化算法求解折扣 {0-1} 背包问题

### 基本信息

- **标题**：《改进蚁群优化算法求解折扣 {0-1} 背包问题》
- **作者**：张铭、邓文瀚、林娟、钟一文
- **发表**：《计算机工程与应用》2021 年第 57 卷第 13 期

### 研究背景

**折扣 {0-1} 背包问题（DKP）** 是一种 NP-困难的组合优化问题，模拟商场促销等折扣场景，在商业决策、资源分配、密码学等领域有应用。

问题设定：

- 背包容量为 `C`；
- 共有 `n` 组物品，每组有 3 个物品；
- 每组中最多只能选 1 个物品放入背包；
- 目标：在不超过背包容量的前提下，使总价值最大。

### 核心贡献：MACO 算法

论文提出了一个求解 DKP 的改进蚁群优化算法 **MACO**，主要创新点：

1. **整数编码 + 组内竞争选择**
   - 采用四进制整数编码：`xi ∈ {0,1,2,3}`，0 表示不选，1/2/3 表示选第 1/2/3 个物品；
   - 使用 `n×4` 的信息素矩阵，每组内 4 种选择互相竞争；
   - 将传统 ACO 全局竞争的 `O(n²)` 时间复杂度降到 `O(n)`。

2. **舍弃启发式信息**
   - 传统 ACO 同时依赖信息素 `τ` 和启发式信息 `η`；
   - MACO 只保留信息素，去掉了 `β` 等参数，简化参数调节；
   - 修复和优化算子已能保证收敛速度，加入启发式信息反而容易早熟。

3. **基于个体历史最优的信息素更新**
   - 每只蚂蚁记录自己的历史最优解 `pbk`；
   - 信息素更新综合考虑个体最优和全局最优 `gb`；
   - 借鉴 MAX-MIN Ant System 思想，把信息素限制在 `[MINPHE, MAXPHE]` 范围内。

4. **混合贪婪修复与优化算子**
   - 现有算法大多只用“价值密度”排序来修复/优化解；
   - 论文发现同时考虑**价值密度和价值**效果更好；
   - 以概率 `pd` 按价值密度排序，以概率 `1-pd` 按价值排序，再调用贪婪优化算子。

### 实验结论

在 DKP 的 4 类标准测试集（UDKP、WDKP、SDKP、IDKP）上，与 FirEGA、GPSO、GTOA、HBDE、SG 等算法对比：

- MACO 在 UDKP、WDKP 上所有实例均取得最优；
- 在 SDKP、IDKP 上绝大多数实例最优；
- Wilcoxon 秩和检验表明 MACO 显著优于其他算法。

---

## 论文二：混合群体增量学习算法求解闭环布局问题

### 基本信息

- **标题**：《混合群体增量学习算法求解闭环布局问题》
- **作者**：邓文瀚、张铭、王李进、钟一文
- **发表**：《计算机应用》2021 年第 41 卷第 1 期

### 研究背景

**闭环布局问题（CLLP）** 是一种 NP-困难的**混合优化问题**，属于设施布局问题（FLP）的一种。

问题设定：

- 有一条大小可调的矩形物料搬运路径（矩形环）；
- 有 `n` 个矩形设施，每个设施尺寸固定；
- 设施可以放在矩形环的 8 个侧面上；
- 设施之间有物料流矩阵 `F`；
- 目标：**确定设施的最佳放置次序 + 矩形环的最佳长宽尺寸**，使总加权运输成本最小。

数学模型：

$$
\min \sum_{i=1}^{n-1}\sum_{j=i+1}^{n} f_{ij} \cdot d_{ij}
$$

其中 `dij` 是设施 `i` 与 `j` 在矩形环上的最短距离。

### 现有方法不足

- MILP 只能解小规模问题；
- SA、MMBO、RKCS 等大多用**枚举法**遍历矩形环尺寸，对每个尺寸再优化设施次序；
- 枚举法产生大量无效计算，效率低，难以扩展。

### 核心贡献：HPBIL 算法

论文提出了 **混合群体增量学习算法（HPBIL）**，把 CLLP 拆成两个子问题同时优化：

1. **离散 PBIL（DPBIL）—— 优化设施放置次序**
   - 用 `n×n` 概率矩阵表示“设施 i 放在第 j 个位置”的概率；
   - 通过轮盘赌采样生成排列型解；
   - 时间复杂度为 `O(n²)`。

2. **连续 PBIL（CPBIL）—— 优化矩形环大小**
   - 用高斯分布 `N(X, σ)` 建模矩形环的水平长度 `h` 和竖直长度 `v`；
   - 根据种群中的最优/最差个体更新均值 `X`；
   - 根据种群方差更新标准差 `σ`。

3. **局部搜索算子（LS）**
   - 每一代选出部分优质解；
   - 先用缩小 `h` 和 `v` 的方式寻找更紧凑的矩形环；
   - 再对设施次序做两两交换的爬山搜索。

### 实验结论

在 13 个标准 CLLP 实例上（规模 4~50 个设施），与 MILP、SA、MMBO、RKCS、DE 对比：

- HPBIL 在 **9 个测试实例上找到了新的最优布局**；
- 在大规模实例 K30、K40、K50 上，HPBIL 比 RKCS 快几个数量级；
- Wilcoxon 秩和检验显示 HPBIL 显著优于 RKCS 和 DE。

---

## 蚁群算法（ACO）详解

### 核心思想

蚁群算法是一种**模拟自然界蚂蚁觅食行为的群智能优化算法**。蚂蚁在寻找食物时会沿途释放**信息素（Pheromone）**，后续蚂蚁更倾向于沿信息素浓度高的路径走，最终群体能找到最短路径。

### 基本流程（以 TSP 为例）

1. **初始化**：在每条路径上放置等量信息素；
2. **构造解**：每只蚂蚁根据信息素浓度和启发式信息选择下一步；
3. **评价解**：计算每只蚂蚁走过的路径总长度；
4. **更新信息素**：
   - 所有路径上的信息素先按一定比例**蒸发**；
   - 再根据解的质量给更好的路径**增加信息素**；
5. **重复迭代**，直到收敛。

### 关键公式

蚂蚁选择下一节点的概率：

$$
P_{ij} = \frac{[\tau_{ij}]^\alpha \cdot [\eta_{ij}]^\beta}{\sum_{j \in \text{候选集}} [\tau_{ij}]^\alpha \cdot [\eta_{ij}]^\beta}
$$

其中：

- $\tau_{ij}$：路径 $(i,j)$ 上的信息素浓度；
- $\eta_{ij}$：启发式信息，通常取距离倒数或价值密度；
- $\alpha$：信息素重要程度；
- $\beta$：启发式信息重要程度。

信息素更新：

$$
\tau_{ij} \leftarrow (1-\rho) \cdot \tau_{ij} + \Delta \tau_{ij}
$$

- $\rho$：信息素蒸发率；
- $\Delta \tau_{ij}$：本次迭代新增的信息素，通常与解的质量成正比。

### 主要变体

| 算法 | 特点 |
|------|------|
| Ant System (AS) | 最原始的蚁群算法 |
| Ant Colony System (ACS) | 加入局部信息素更新，收敛更快 |
| MAX-MIN Ant System (MMAS) | 限制信息素上下界，防止早熟 |
| Elitist AS | 给最优解额外奖励信息素 |

### 优点与缺点

**优点**：

- 全局搜索能力强；
- 适合离散组合优化；
- 天然并行，容易分布式实现；
- 容易与其他算法混合。

**缺点**：

- 参数较多（α、β、ρ、蚂蚁数等），调参麻烦；
- 容易陷入局部最优；
- 大规模问题收敛速度可能较慢。

---

## PBIL 算法详解

### 基本思想

PBIL（Population-Based Incremental Learning）是一种**基于概率模型的进化算法**，属于**分布估计算法（EDA）**家族。它由 Baluja 于 1994 年提出。

核心思想：

> 不再直接保存和操作种群，而是维护一个**概率向量（或概率模型）**来描述整个种群的分布。每一代从概率模型中采样生成新种群，然后根据优质解来更新概率模型，最终概率模型收敛到优质解区域。

### 基本流程（二进制优化问题）

假设优化一个长度为 `n` 的二进制串：

$$
X = (x_1, x_2, ..., x_n), \quad x_i \in \{0, 1\}
$$

维护概率向量：

$$
P = (p_1, p_2, ..., p_n)
$$

其中 $p_i$ 表示第 `i` 位取值为 1 的概率。

算法流程：

```
1. 初始化概率向量：p_i = 0.5
2. while 没有达到停止条件 do
3.   根据 P 采样生成 N 个个体
4.   评价这 N 个个体的适应度
5.   选出 M 个最优个体（优势个体）
6.   用这 M 个优势个体更新概率向量 P
7. end while
8. 返回历史最优解
```

### 核心更新公式

$$
p_i \leftarrow (1 - \alpha) \cdot p_i + \alpha \cdot \frac{1}{M} \sum_{k=1}^{M} x_i^{(k)}
$$

其中：

- $p_i$：第 `i` 位取 1 的概率；
- $\alpha$：学习率；
- $M$：优势个体数量；
- $x_i^{(k)}$：第 `k` 个优势个体的第 `i` 位取值。

### PBIL 的扩展

1. **离散 PBIL（DPBIL）**
   - 用于排列型/组合优化问题；
   - 使用 `n×n` 概率矩阵；
   - `pm[i][j]` 表示“设施 i 放在第 j 个位置”的概率。

2. **连续 PBIL（CPBIL）**
   - 用于连续优化问题；
   - 用高斯分布 `N(μ, σ)` 建模每个变量；
   - 根据优势个体更新均值 `μ` 和方差 `σ`。

### PBIL 与 GA、EDA 的关系

| 特点 | 遗传算法 GA | PBIL |
|------|------------|------|
| 操作对象 | 个体 | 概率模型 |
| 信息保存 | 种群基因 | 概率向量/矩阵 |
| 遗传算子 | 选择、交叉、变异 | 采样 + 概率更新 |
| 优势 | 搜索能力强 | 收敛快、参数少 |
| 劣势 | 参数多、易早熟 | 对变量依赖建模弱 |

### 优点与缺点

**优点**：

- 收敛速度快；
- 参数相对较少；
- 全局搜索能力强；
- 容易扩展到连续/离散/排列问题；
- 可与其他算法混合。

**缺点**：

- 基本 PBIL 假设变量独立，无法捕捉变量间依赖；
- 学习率过大时容易早熟；
- 对复杂问题建模能力有限；
- 概率可能边界化，难以跳出。

### 应用领域

- 旅行商问题（TSP）
- 背包问题
- 设施布局问题（如 CLLP）
- 特征选择
- 调度问题
- 本体匹配
- 连续函数优化
- 多目标优化

### 一句话总结

> **PBIL 算法用一个概率向量（或概率分布）来刻画“好解”的特征，通过不断采样优质解并更新概率模型，使搜索逐步收敛到全局优质区域。**

---

*文档生成时间：2026-07-06*

---

## 进化算法（Evolutionary Algorithms, EA）与遗传算法（Genetic Algorithm, GA）详解

### 1. 进化算法概述

**进化算法（Evolutionary Algorithms, EA）** 是一类模拟自然界生物进化过程的优化算法。它把问题的候选解看作“个体”，把候选解的集合看作“种群”，通过模拟自然选择、遗传和变异的机制，使种群逐代进化，最终找到近似最优解。

进化算法的核心思想来源于达尔文的**自然选择学说**：

- **适者生存**：适应度高的个体更容易存活和繁殖；
- **遗传**：后代继承父母的优良特征；
- **变异**：后代可能产生新的特征，增加种群多样性。

#### 1.1 进化算法的主要分支

| 算法 | 英文 | 特点 |
|---|---|---|
| 遗传算法 | Genetic Algorithm (GA) | 以染色体编码、选择、交叉、变异为核心 |
| 遗传规划 | Genetic Programming (GP) | 进化计算机程序或表达式树 |
| 进化策略 | Evolution Strategy (ES) | 强调自适应性变异，常用于连续优化 |
| 进化规划 | Evolutionary Programming (EP) | 主要使用变异算子 |
| 差分进化 | Differential Evolution (DE) | 通过向量差分产生变异，连续优化效果好 |
| 分布估计算法 | Estimation of Distribution Algorithm (EDA) | 用概率模型替代交叉变异 |

遗传算法（GA）是进化算法家族中**最经典、应用最广泛**的代表。

---

### 2. 遗传算法（GA）详解

#### 2.1 基本思想

遗传算法把问题的解编码成一条“染色体”（通常是一串二进制、整数或实数），然后通过以下操作不断进化：

1. **选择（Selection）**：从当前种群中挑选适应度高的个体；
2. **交叉（Crossover）**：两个父代个体交换部分基因，产生子代；
3. **变异（Mutation）**：随机改变个体的某些基因，引入多样性；
4. **替换（Replacement）**：用子代替换部分或全部父代，形成新种群。

#### 2.2 算法流程

```
1. 初始化：随机生成 N 个个体，构成初始种群
2. 评价：计算每个个体的适应度
3. while 没有达到停止条件 do
4.   选择：根据适应度从种群中选择父代个体
5.   交叉：以概率 Pc 对父代进行交叉，产生子代
6.   变异：以概率 Pm 对子代进行变异
7.   评价：计算子代的适应度
8.   替换：用子代替换部分或全部旧个体，形成新种群
9. end while
10. 返回历史最优个体
```

#### 2.3 编码方式

| 编码方式 | 适用场景 | 示例 |
|---|---|---|
| 二进制编码 | 组合优化、0-1 问题 | `10110010` |
| 整数编码 | 排列问题、离散选择 | `[1, 3, 2, 5, 4]` |
| 实数编码 | 连续函数优化 | `[3.14, -0.5, 2.71]` |
| 树形编码 | 遗传规划、表达式进化 | `(* (+ x 1) y)` |

#### 2.4 适应度函数

适应度函数 `f(x)` 用于评价个体的好坏，是算法搜索的导向。设计合理的适应度函数是遗传算法成功的关键。

对于最小化问题，通常需要转换为最大化问题：

$$
fitness(x) = \frac{1}{f(x) + \epsilon}
$$

或直接取负数。

#### 2.5 选择算子

| 选择方法 | 说明 | 特点 |
|---|---|---|
| 轮盘赌选择 | 按适应度比例选择 | 简单，但可能早熟 |
| 锦标赛选择 | 随机选 k 个个体，取最优 | 最常用，鲁棒性好 |
| 排序选择 | 按适应度排序后选择 | 避免适应度比例差异过大 |
| 精英保留 | 直接把最优个体保留到下一代 | 防止最优解丢失 |

#### 2.6 交叉算子

| 交叉方式 | 适用编码 | 说明 |
|---|---|---|
| 单点交叉 | 二进制/整数 | 随机选一个断点，交换后半部分 |
| 两点交叉 | 二进制/整数 | 随机选两个断点，交换中间部分 |
| 均匀交叉 | 二进制 | 每个基因独立决定是否交换 |
| 部分映射交叉（PMX） | 排列编码 | 保持排列合法性，常用于 TSP |
| 顺序交叉（OX） | 排列编码 | 保持子序列相对顺序 |
| 算术交叉 | 实数编码 | 按权重线性组合两个父代 |

#### 2.7 变异算子

| 变异方式 | 适用编码 | 说明 |
|---|---|---|
| 位翻转变异 | 二进制 | 随机把 0 变 1 或 1 变 0 |
| 交换变异 | 排列 | 随机交换两个位置的值 |
| 插入变异 | 排列 | 随机取一个值插入到另一位置 |
| 高斯变异 | 实数编码 | 加上一个高斯分布随机数 |
| 均匀变异 | 实数编码 | 在取值范围内随机替换 |

#### 2.8 遗传算法的核心参数

| 参数 | 符号 | 典型取值 | 作用 |
|---|---|---|---|
| 种群规模 | N | 50~500 | 个体数量 |
| 交叉概率 | Pc | 0.6~0.9 | 控制交叉发生频率 |
| 变异概率 | Pm | 0.001~0.1 | 控制变异发生频率 |
| 选择压力 | — | — | 控制优秀个体被选中概率 |
| 精英个体数 | — | 1~5 | 直接保留的最优个体数 |
| 终止代数 | G | 100~10000 | 最大迭代次数 |

---

### 3. 遗传算法的理论基础

#### 3.1 模式定理（Schema Theorem）

Holland 提出的模式定理是遗传算法的理论基础：

> **适应度高、长度短、阶数低的模式（Schema）在种群中的数量会呈指数级增长。**

- **模式**：染色体中某些固定位置上的特定基因组合，如 `1*0*1`（`*` 表示任意）；
- **阶数**：模式中固定位置的个数；
- **定义长度**：模式中第一个和最后一个固定位置之间的距离。

模式定理解释了为什么 GA 能有效搜索：短而优的模式会不断被选择和组合，逐步构建出更好的解。

#### 3.2 积木块假设

> 遗传算法通过组合低阶、高适应度的“积木块”（Building Blocks），逐步构造出高阶、高适应度的个体。

这解释了交叉算子的重要性：交叉能把不同个体上的优良基因块组合到一起。

---

### 4. 遗传算法的优缺点

#### 优点

1. **全局搜索能力强**：种群并行搜索多个区域，不易陷入局部最优；
2. **通用性好**：几乎适用于所有优化问题，只需设计编码和适应度函数；
3. **不依赖梯度信息**：适合不可导、不连续、黑箱问题；
4. **可并行化**：每个个体独立评价，适合大规模并行；
5. **可与其他算法混合**：如遗传蚁群算法、遗传模拟退火算法等。

#### 缺点

1. **参数敏感**：种群规模、交叉率、变异率都需要仔细调整；
2. **编码设计困难**：问题编码方式直接影响性能；
3. **收敛速度慢**：复杂问题可能需要大量代数；
4. **早熟收敛**：种群多样性过早丧失，陷入局部最优；
5. **理论分析复杂**：虽然模式定理有解释，但实际行为仍难精确预测。

---

### 5. 遗传算法的常见改进

| 改进方向 | 代表算法 | 核心思想 |
|---|---|---|
| 自适应参数 | 自适应 GA | 根据种群状态动态调整交叉/变异概率 |
| 多种群协同 | 多种群 GA | 多个子种群独立进化并定期迁移 |
| 混合算法 | GA + 局部搜索 | 用局部搜索增强求精能力 |
| 量子遗传 | QGA | 用量子比特表示叠加态 |
| 差分进化 | DE | 用向量差分代替传统交叉 |
| 遗传规划 | GP | 进化程序结构或数学表达式 |

---

### 6. 进化算法与遗传算法的关系

可以用下面这个关系理解：

```
进化计算（Evolutionary Computation, EC）
├── 遗传算法（GA）
├── 遗传规划（GP）
├── 进化策略（ES）
├── 进化规划（EP）
└── 其他变体（DE、EDA、Memetic Algorithm 等）
```

- **进化计算（EC）** 是最大范畴；
- **进化算法（EA）** 是 EC 中基于种群迭代进化的算法总称；
- **遗传算法（GA）** 是最经典、最具代表性的 EA。

---

### 7. Python 示例

```python
import numpy as np
import random

# 示例：求解函数 f(x) = x^2 在 x ∈ [-10, 10] 上的最小值

def fitness(x):
    # 求最小值，所以适应度取倒数（越大越好）
    return 1 / (x**2 + 1e-6)

# 二进制编码参数
N = 50          # 种群规模
GENES = 16      # 染色体长度
PC = 0.8        # 交叉概率
PM = 0.01       # 变异概率
G = 100         # 迭代次数

def decode(binary):
    # 把二进制串解码为 [-10, 10] 的实数
    val = int(''.join(map(str, binary)), 2)
    return -10 + 20 * val / (2**GENES - 1)

def init_population():
    return [np.random.randint(0, 2, GENES).tolist() for _ in range(N)]

def select(pop, fits):
    # 锦标赛选择
    idx1, idx2 = random.sample(range(N), 2)
    return pop[idx1] if fits[idx1] > fits[idx2] else pop[idx2]

def crossover(p1, p2):
    if random.random() < PC:
        point = random.randint(1, GENES - 1)
        return p1[:point] + p2[point:], p2[:point] + p1[point:]
    return p1[:], p2[:]

def mutate(ind):
    return [1 - g if random.random() < PM else g for g in ind]

# 初始化
pop = init_population()
best_ind = None
best_fit = -np.inf

for gen in range(G):
    fits = [fitness(decode(ind)) for ind in pop]
    
    # 记录最优
    for i, fit in enumerate(fits):
        if fit > best_fit:
            best_fit = fit
            best_ind = pop[i][:]
    
    # 生成新种群
    new_pop = []
    while len(new_pop) < N:
        p1 = select(pop, fits)
        p2 = select(pop, fits)
        c1, c2 = crossover(p1, p2)
        c1 = mutate(c1)
        c2 = mutate(c2)
        new_pop.extend([c1, c2])
    
    pop = new_pop[:N]

print("最优解 x:", decode(best_ind))
print("最优值 f(x):", decode(best_ind)**2)
```

---

### 8. 应用领域

- 函数优化
- 组合优化（TSP、背包问题、调度问题）
- 机器学习（特征选择、神经网络权重训练）
- 工程设计优化
- 图像处理
- 路径规划
- 金融投资组合优化
- 生物信息学

---

### 9. 一句话总结

> **进化算法模拟自然界的“适者生存、优胜劣汰”机制，通过选择、交叉、变异等操作使种群逐代进化；遗传算法是其中最经典的代表，它把解编码成染色体，利用遗传操作搜索全局最优解。**

---

## 粒子群优化算法（Particle Swarm Optimization, PSO）详解

### 1. 算法起源与思想

**粒子群优化算法（Particle Swarm Optimization, PSO）** 是一种基于群体智能的随机优化算法，由 Kennedy 和 Eberhart 于 1995 年提出，灵感来源于**鸟群觅食**和**鱼群聚集**等群体行为。

基本观察：

- 每只鸟/鱼在搜索食物时，不仅根据自己的经验，还会参考群体中其他个体的信息；
- 整个群体通过简单的局部交互，能够涌现出协调的全局搜索行为。

PSO 把这种思想抽象为：

- 每个候选解称为一个**粒子（Particle）**；
- 所有粒子组成一个**种群/鸟群（Swarm）**；
- 每个粒子在解空间中飞行，通过不断更新自己的速度和位置来寻找最优解。

### 2. 基本概念

每个粒子有两个重要属性：

1. **位置 `x_i`**：当前解；
2. **速度 `v_i`**：决定粒子下一次移动的方向和步长。

每个粒子还记录两个历史信息：

1. **个体历史最优位置 `pbest_i`**：该粒子自身经历过的最好位置；
2. **群体历史最优位置 `gbest`**：整个种群中所有粒子经历过的最好位置。

### 3. 速度与位置更新公式

在标准 PSO 中，粒子的速度和位置按以下公式更新：

$$
v_i(t+1) = w \cdot v_i(t) + c_1 \cdot r_1 \cdot (pbest_i - x_i(t)) + c_2 \cdot r_2 \cdot (gbest - x_i(t))
$$

$$
x_i(t+1) = x_i(t) + v_i(t+1)
$$

其中：

- $v_i(t)$：粒子 `i` 在第 `t` 代的速度；
- $x_i(t)$：粒子 `i` 在第 `t` 代的位置；
- $w$：**惯性权重**，控制粒子保持先前速度的程度；
- $c_1$：**认知系数**，控制粒子向自身最优学习的程度；
- $c_2$：**社会系数**，控制粒子向群体最优学习的程度；
- $r_1, r_2$：`[0,1]` 之间的随机数。

### 4. 三项的物理意义

| 项 | 名称 | 作用 |
|---|---|---|
| $w \cdot v_i(t)$ | 惯性项 | 保持粒子原有运动趋势，平衡全局与局部搜索 |
| $c_1 r_1 (pbest_i - x_i)$ | 认知项 | 粒子飞向自己历史最优位置 |
| $c_2 r_2 (gbest - x_i)$ | 社会项 | 粒子飞向群体历史最优位置 |

### 5. 算法流程

```
1. 初始化粒子群：
   - 随机生成每个粒子的位置 x_i 和速度 v_i
   - 初始化 pbest_i = x_i
   - 初始化 gbest = 所有粒子中的最优位置

2. while 没有达到停止条件 do
3.   for 每个粒子 i do
4.     根据速度公式更新 v_i
5.     根据位置公式更新 x_i
6.     评价新位置 f(x_i)
7.     if f(x_i) 优于 f(pbest_i) then
8.        pbest_i = x_i
9.     end if
10.    if f(x_i) 优于 f(gbest) then
11.       gbest = x_i
12.    end if
13.  end for
14. end while

15. 返回 gbest
```

### 6. 关键参数

| 参数 | 典型取值 | 作用 |
|---|---|---|
| 种群规模 `N` | 20~100 | 粒子数量 |
| 惯性权重 `w` | 0.4~0.9 | 控制全局/局部搜索能力 |
| 认知系数 `c_1` | 1.5~2.0 | 个体经验影响 |
| 社会系数 `c_2` | 1.5~2.0 | 群体经验影响 |
| 最大速度 `v_max` | 视问题而定 | 防止粒子飞出过远 |

### 7. 惯性权重的常见策略

1. **固定 `w`**：简单但效果一般；
2. **线性递减**：

$$
w(t) = w_{max} - \frac{w_{max} - w_{min}}{T_{max}} \cdot t
$$

   早期 `w` 大，侧重全局探索；后期 `w` 小，侧重局部开发。

3. **自适应 `w`**：根据粒子收敛程度动态调整。

### 8. PSO 的变体

#### 8.1 二进制 PSO（BPSO）

用于离散优化问题，位置只取 0 或 1。通过 Sigmoid 函数将速度映射为概率：

$$
S(v_{ij}) = \frac{1}{1 + e^{-v_{ij}}}
$$

$$
x_{ij} = \begin{cases} 1, & \text{if } rand() < S(v_{ij}) \\ 0, & \text{otherwise} \end{cases}
$$

#### 8.2 离散 PSO

用于排列型问题，如 TSP、作业调度。通过重新定义速度、位置和减法的含义来实现。

#### 8.3 局部版本 PSO（lbest）

每个粒子只参考**邻域内最优**，而不是全局最优。常用拓扑结构：

- 环形拓扑（Ring）；
- 轮形拓扑（Wheel）；
- 星形拓扑（Star，即 gbest）。

#### 8.4 其他改进

- **压缩因子 PSO**：用收缩系数保证收敛；
- **完全 informed PSO（FIPS）**：粒子参考所有邻居的信息；
- **自适应 PSO（APSO）**：参数自适应调整；
- **多目标 PSO（MOPSO）**：用于多目标优化。

### 9. 优点与缺点

#### 优点

1. **原理简单**：只有两个核心公式；
2. **参数少**：相比 GA、ACO，需要调节的参数更少；
3. **收敛速度快**：信息共享机制使粒子能快速向优解聚集；
4. **通用性强**：容易扩展到连续、离散、多目标问题；
5. **并行性好**：每个粒子独立更新，适合并行计算。

#### 缺点

1. **容易早熟收敛**：粒子容易快速聚集到局部最优；
2. **多样性保持不足**：后期种群多样性下降；
3. **参数敏感**：`w`、`c1`、`c2` 对性能影响大；
4. **高维问题表现下降**：在复杂高维问题上可能收敛慢或陷入局部最优。

### 10. PSO 与 GA、ACO 的对比

| 算法 | 信息共享方式 | 收敛速度 | 全局搜索 | 易早熟 |
|---|---|---|---|---|
| GA | 交叉、变异 | 中等 | 较强 | 中等 |
| ACO | 信息素 | 较慢 | 强 | 中等 |
| PSO | pbest/gbest | 快 | 中等 | 较易 |

### 11. Python 示例

```python
import numpy as np
import random

# 示例：求解函数 f(x, y) = x^2 + y^2 的最小值
def f(x):
    return x[0]**2 + x[1]**2

# 参数
N = 30          # 粒子数
dim = 2         # 维度
T = 100         # 迭代次数
w = 0.8         # 惯性权重
c1 = 1.5        # 认知系数
c2 = 1.5        # 社会系数
v_max = 1.0     # 最大速度

# 初始化
x = np.random.uniform(-10, 10, (N, dim))
v = np.random.uniform(-1, 1, (N, dim))
pbest = x.copy()
gbest = x[np.argmin([f(p) for p in x])].copy()

for t in range(T):
    for i in range(N):
        # 更新速度
        r1, r2 = random.random(), random.random()
        v[i] = (w * v[i]
                + c1 * r1 * (pbest[i] - x[i])
                + c2 * r2 * (gbest - x[i]))
        # 限制速度
        v[i] = np.clip(v[i], -v_max, v_max)
        
        # 更新位置
        x[i] = x[i] + v[i]
        
        # 更新个体最优
        if f(x[i]) < f(pbest[i]):
            pbest[i] = x[i].copy()
            
            # 更新群体最优
            if f(pbest[i]) < f(gbest):
                gbest = pbest[i].copy()

print("最优解:", gbest)
print("最优值:", f(gbest))
```

### 12. 应用领域

- 函数优化
- 神经网络训练
- 特征选择
- 图像处理
- 电力系统优化
- 机器人路径规划
- 调度问题
- 参数调优
- 多目标优化

### 13. 一句话总结

> **粒子群优化算法模拟鸟群/鱼群的群体协作行为，每个粒子根据自身经验和群体经验不断调整飞行方向和速度，最终在解空间中收敛到最优区域。**

---

## 模拟退火算法（Simulated Annealing, SA）详解

### 1. 算法起源与思想

**模拟退火算法（Simulated Annealing, SA）** 是一种**基于概率的元启发式优化算法**，由 Kirkpatrick、Gelatt 和 Vecchi 于 1983 年提出，灵感来源于**固体退火过程**。

在物理学中，退火是指：

1. 将固体加热到高温，使内部粒子剧烈运动；
2. 然后缓慢降温；
3. 如果降温足够慢，粒子有机会重新排列，最终达到能量最低的稳定状态（即最优结构）。

SA 把这一物理过程抽象为优化过程：

- **温度 `T`**：控制搜索的随机性；
- **能量 `E`**：对应优化问题的目标函数值；
- **降温过程**：温度逐渐降低，搜索从“大范围探索”过渡到“局部精细搜索”。

### 2. 核心机制

SA 与贪婪算法的最大区别在于：

> **SA 允许以一定概率接受比当前解更差的解**，从而避免陷入局部最优。

#### 2.1 Metropolis 准则

判断是否接受新解的概率由 **Metropolis 准则** 决定：

设当前解为 $s$，新解为 $s'$，目标函数为 $f$（这里假设求最小值），则：

- 如果 $\Delta E = f(s') - f(s) < 0$（新解更好），**一定接受**；
- 如果 $\Delta E \geq 0$（新解更差），以概率接受：

$$
P(\text{接受}) = e^{-\frac{\Delta E}{T}}
$$

其中：

- $\Delta E$：目标函数差值；
- $T$：当前温度；
- $k_B$：玻尔兹曼常数，在算法中通常简化为 1。

**直观理解**：

- 温度高时，$T$ 很大，$e^{-\Delta E/T}$ 接近 1，即使差很多也容易被接受；
- 温度低时，$T$ 很小，$e^{-\Delta E/T}$ 接近 0，基本只接受好解；
- 同样的 $\Delta E$，温度越高越容易被接受。

### 3. 算法流程

```
1. 初始化：
   - 随机生成一个初始解 s
   - 设置初始温度 T0
   - 设置终止温度 T_min
   - 设置降温系数 alpha（0 < alpha < 1）
   - 设置每个温度下的迭代次数 L

2. while T > T_min do
3.   for i = 1 to L do
4.     从当前解 s 的邻域中随机产生一个新解 s'
5.     计算 ΔE = f(s') - f(s)
6.     if ΔE < 0 then
7.        s = s'
8.     else
9.        以概率 e^(-ΔE/T) 接受 s'，即 s = s'
10.    end if
11.  end for
12.  T = alpha * T   // 降温
13. end while

14. 返回最优解
```

### 4. 关键参数

| 参数 | 含义 | 影响 |
|------|------|------|
| 初始温度 $T_0$ | 算法开始时的温度 | 太高浪费计算，太低容易早熟 |
| 终止温度 $T_{min}$ | 算法停止的温度 | 通常设为一个很小的正数 |
| 降温系数 $\alpha$ | 每次降温的比例 | 越接近 1 降温越慢，搜索越充分 |
| 每个温度的迭代次数 $L$ | 每个温度下产生多少个新解 | 太少不够稳定，太多浪费时间 |
| 邻域生成方式 | 如何从当前解产生新解 | 决定搜索空间和效率 |

### 5. 邻域生成方式示例

不同问题有不同的邻域生成方式：

- **TSP**：交换两个城市的位置、翻转一段路径；
- **0-1 背包问题**：随机改变某个物品的选中状态；
- **CLLP**：交换两个设施的位置、改变矩形环尺寸；
- **函数优化**：在当前点周围随机扰动。

### 6. 与爬山算法的对比

| 特点 | 爬山算法 | 模拟退火算法 |
|------|----------|--------------|
| 接受劣解 | 从不接受 | 以一定概率接受 |
| 全局搜索能力 | 弱，易陷入局部最优 | 强 |
| 收敛速度 | 快 | 较慢 |
| 参数 | 少 | 较多 |
| 适用场景 | 单峰问题 | 多峰、复杂优化问题 |

### 7. 优点与缺点

#### 优点

1. **全局搜索能力强**：通过概率接受劣解，能跳出局部最优；
2. **通用性好**：几乎适用于所有组合优化和连续优化问题；
3. **实现简单**：核心逻辑非常清晰；
4. **理论基础扎实**：有统计力学和马尔可夫链理论支撑；
5. **可与其他算法结合**：常与遗传算法、局部搜索等混合使用。

#### 缺点

1. **参数敏感**：初始温度、降温系数、迭代次数都需要仔细设计；
2. **收敛速度慢**：为了保证全局收敛，往往需要很长的计算时间；
3. **降温 schedule 设计困难**：降温太快容易早熟，太慢效率低；
4. **邻域设计依赖问题**：不同问题需要设计不同的邻域生成方式。

### 8. 常见的降温策略

1. **指数降温**：$T_{k+1} = \alpha \cdot T_k$，最常用；
2. **线性降温**：$T_{k+1} = T_k - \Delta T$；
3. **对数降温**：$T_k = \frac{T_0}{\ln(1+k)}$，理论上保证收敛；
4. **自适应降温**：根据接受率动态调整降温速度。

### 9. Python 示例

```python
import math
import random
import numpy as np

# 示例：求解函数 f(x) = x^2 的最小值，x ∈ [-10, 10]
def f(x):
    return x ** 2

def neighbor(x):
    # 在当前解附近随机扰动
    return x + random.uniform(-1, 1)

# 参数设置
T0 = 100.0
T_min = 1e-4
alpha = 0.95
L = 100

# 初始解
x = random.uniform(-10, 10)
best_x = x
best_f = f(x)
T = T0

while T > T_min:
    for i in range(L):
        x_new = neighbor(x)
        delta = f(x_new) - f(x)
        
        if delta < 0 or random.random() < math.exp(-delta / T):
            x = x_new
            if f(x) < best_f:
                best_x = x
                best_f = f(x)
    
    T = alpha * T

print("最优解:", best_x)
print("最优值:", best_f)
```

### 10. 应用领域

- 旅行商问题（TSP）
- 车辆路径问题（VRP）
- 设施布局问题（如 CLLP）
- 调度问题
- 集成电路布局
- 神经网络训练
- 蛋白质折叠
- 函数优化

### 11. 一句话总结

> **模拟退火算法通过模拟物理退火过程，在高温时大胆探索、低温时精细搜索，并以一定概率接受劣解，从而跳出局部最优，寻找全局近似最优解。**
