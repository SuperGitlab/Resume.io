[project]
name = "dengwenhan-home"
version = "0.1.0"
description = "邓文瀚个人主页 - FastAPI 服务"
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]",
    "uvicorn[standard]",
    "markdown",
    "pymdown-extensions",
    "pygments",
]

[tool.uv]
package = false
