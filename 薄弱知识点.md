# 1.依赖注入

````python
依赖注入是一种设计模式，核心是把对象所需的依赖从外部传入，而不是在对象内部创建。它实现了控制反转，让对象之间解耦，便于测试和替换依赖。在 FastAPI 中，Depends() 就是依赖注入的实现，框架会自动解析依赖关系，把需要的对象注入到路由函数中。
````
✅ 优点（为什么大家都爱用它）
1. 彻底干掉重复代码（DRY 原则）
这是最直观的好处。就像我们之前对比的，如果没有 Depends，你需要在 100 个接口里重复写“取请求头 -> 判空 -> 查数据库”的代码。有了 Depends，只需写 1 次，维护成本直降 99%。

2. 自动的“懒加载”与“请求上下文”
它能精确捕获“用户发请求的那一瞬间”传来的数据（Header、Query、Body）。这是手动 chef = Chef(...) 永远做不到的——因为后者在启动时就固化了。

3. 依赖链的自动解析（叠叠乐）
如果你的验证需要查数据库，而数据库需要连接池，你可以这样写：
````python
async def get_db():  # 依赖1：拿数据库连接
    return DatabaseSession()

async def get_current_user(db = Depends(get_db), token = Header(...)):  # 依赖2：依赖1
    return await db.query_user(token)

@app.get("/tasks")
def task(user = Depends(get_current_user)):  # 依赖3：依赖2
    ...
````
你只需要在接口里写 Depends(get_current_user)，FastAPI 会自动帮你去调用 get_db，并把结果传进 get_current_user。手动构造这些链式对象会非常繁琐，而 Depends 让这一切变得极其丝滑。

4. 缓存机制（提升性能）
正如之前提到的，在同一个请求中，如果你多次调用 Depends(get_current_user)，FastAPI 默认只会执行第一次，后面的直接拿缓存结果。这避免了在同一个请求里反复查数据库或解密 JWT，性能拉满。

5. 测试时易于替换（Mock 利器）
FastAPI 提供了 app.dependency_overrides，你可以在测试代码中把 get_current_user 替换成一个“假的”函数，直接返回固定的测试用户。这样测试接口时，完全不需要模拟复杂的请求头或数据库，测试代码极其干净。

❌ 缺点（使用时要警惕的坑）
1. 隐式的“魔法”增加了学习门槛（你之前的困惑就源于此）
对于新手来说，Depends 打破了 Python 自上而下的执行直觉。你看着函数定义，很难一眼看出它到底什么时候执行、参数从哪里来。调试时，如果依赖链很长（A 依赖 B，B 依赖 C），一旦报错，堆栈信息会变得很深，排查问题比较费劲。

2. 每次请求都有函数调用开销（微性能损耗）
手动写法 chef = Chef() 在启动时只执行一次，而 Depends 修饰的函数每次请求都会执行一次。虽然 Python 函数调用本身很快，但如果你的依赖函数里什么都没做（比如只返回一个固定配置），依然每次请求都跑一遍，这会造成不必要的 CPU 损耗。

3. 不适用于“重量级”全局对象
假设你的依赖函数是 get_ai_model()，里面要加载一个 5GB 的深度学习模型。
````python
# 错误示范！
async def get_ai_model():  
    return load_huge_model()  # 每次请求都加载一次？服务器直接卡死！

@app.post("/ai")
def predict(model = Depends(get_ai_model)): ...
````
这种情况下绝对不能用 Depends，因为每个请求来都会尝试加载模型，服务器瞬间崩溃。此时必须用启动时加载一次的全局变量 model = load_huge_model()。

4. 难以在“后台任务”或“脚本”中复用
如果你写了一个定时脚本（不经过 HTTP 请求），想复用 get_current_user 的逻辑，你会发现很麻烦，因为 Depends 强依赖 FastAPI 的请求上下文（Request Context）。你没法在脚本里直接调用它，除非模拟一个假的请求对象

💡 总结：什么时候该用，什么时候不该用？
| 场景 | 是否该用 Depends | 推荐做法 |
| --- | --- | --- |
| 读取请求头/Query/Body | ✅ 必须用 | 只有 Depends 能拿到请求时刻的动态数据 |
| 验证用户 Token / 权限 | ✅ 必须用 | 利用缓存和链式解析，非常优雅 |
| 创建数据库会话（Session） | ✅ 推荐用 | 利用依赖链自动关闭连接，省心 |
| 加载配置文件 / 常量 | ❌ 别用 | 启动时 config = load_config() 加载一次，全局复用 |
| 加载 AI 模型 / 大缓存 | ❌ 千万别用 | 启动时全局加载，否则每个请求都卡死 |
| 需要在 Celery/脚本里复用逻辑 | ❌ 别用 | 把核心业务逻辑抽成普通函数，Depends 只做薄薄的一层适配器 |

-------
一句话总结：Depends 是 Web 请求的“专属服务员”，专门负责接待客人（请求）时拿数据、做验证。如果是“后台搬砖的固定重资产”（大模型、配置），就别让服务员去碰。


# 2. asyncio.gather() 与 asyncio.wait() 的区别  详解
### 1. 最核心的区别（一句话总结）

| 特性 | `asyncio.gather()` | `asyncio.wait()` |
|---|---|---|
| 返回值 | 直接返回任务执行的结果列表（按传入顺序） | 返回一个包含两个集合的元组 `(done, pending)` |
| 设计目的 | 我要并发执行，并拿到所有结果 | 我要监控任务状态，根据完成情况做不同操作 |
| 输入要求 | 可以直接传入协程（`async def`），会自动包装成任务 | 必须传入 `Future` 或 `Task` 对象（即要先 `create_task`） |
| 等待条件 | 只能等待 全部完成（除非取消） | 可以等待 全部完成、第一个完成、第一个报错 |
---
### 2. 返回值差异（最直观）
gather：直接给你结果列表
````python
import asyncio

async def task_a():
    await asyncio.sleep(1)
    return "A结果"

async def task_b():
    await asyncio.sleep(2)
    return "B结果"

async def main():
    # gather 直接返回结果，顺序和传入顺序一致
    results = await asyncio.gather(task_a(), task_b())
    print(results)  # 输出: ['A结果', 'B结果']
````
wait：给你两个“任务集合”
````python
async def main():
    # wait 必须传入已经创建好的任务
    t1 = asyncio.create_task(task_a())
    t2 = asyncio.create_task(task_b())
    
    done, pending = await asyncio.wait([t1, t2])
    
    # 你需要手动遍历 done 集合，调用 result() 拿结果
    for task in done:
        print(task.result())  # 输出顺序不确定（谁先完成谁先打印）
````

### 3. 等待条件差异（wait 的独门绝技）
gather 只能傻等所有任务结束，而 wait 可以设置返回条件，这是它最强大的地方：
````python
# 等待所有任务完成（默认，等同于 gather）
done, pending = await asyncio.wait(tasks, return_when=asyncio.ALL_COMPLETED)

# 只要有任意一个任务完成，就立刻返回（超时控制利器）
done, pending = await asyncio.wait(tasks, return_when=asyncio.FIRST_COMPLETED)

# 只要有任意一个任务报错，就立刻返回（竞速失败处理）
done, pending = await asyncio.wait(tasks, return_when=asyncio.FIRST_EXCEPTION)
````
实战场景： 你想同时从 3 个网站获取数据，谁先返回就用谁的结果（竞速），此时 wait 是唯一选择，gather 做不到。

### 4. 异常处理差异
gather 的“一键收集”模式：

默认情况下，只要有一个任务报错，gather 就会抛出异常，其他正在运行的任务不会被取消（会被屏蔽，继续跑完）。

如果加上 return_exceptions=True，异常会像普通结果一样放入列表，不会中断。
````python
results = await asyncio.gather(task_a(), task_b(), return_exceptions=True)
# 如果 task_a 报错，results[0] 就是那个异常对象，程序不会崩溃
````

wait 的“手动体检”模式：

它不会主动抛出异常，你需要遍历 done 集合，手动调用 task.exception() 来检查。
````python
done, pending = await asyncio.wait(tasks)
for task in done:
    if task.exception():
        print(f"出错了: {task.exception()}")  # 你自己决定怎么处理
    else:
        print(task.result())
````

### 5. 取消与超时控制
超时：两者都支持 timeout 参数，但行为略有不同。gather 超时会抛出 TimeoutError 并取消内部任务；wait 超时会直接返回当前的 (done, pending)，不会报错，由你决定如何处理 pending 中的任务。

取消：取消 gather 会取消其中所有尚未完成的任务。取消 wait 不会影响内部任务，它们会继续在后台运行。

### 6. 究竟该用哪个？（开发铁律）

| 你的需求 | 推荐工具 |
|---|---|
| 并发执行多个任务，需要拿到所有返回结果 | ✅ `asyncio.gather()`（代码最简洁） |
| 任务间相互独立，一个失败不影响其它（批量请求） | ✅ `asyncio.gather(..., return_exceptions=True)` |
| 并发多个任务，谁先完成就用谁的（例如超时降级） | ✅ `asyncio.wait(..., return_when=FIRST_COMPLETED)` |
| 需要精细控制任务状态，比如定时检查哪些还在跑 | ✅ `asyncio.wait()`（因为它返回 pending 集合） |
| 需要限制并发数量（比如同时只能跑 10 个） | ❌ 两者都做不到，需要用 `asyncio.Semaphore` 或 `asyncio.TaskGroup`（Python 3.11+） |
-----
### 总结一句话

如果你的代码里写 await asyncio.gather(...)，意思通常是：“你们几个一起跑，跑完把结果按顺序拿给我。”

如果你的代码里写 done, pending = await asyncio.wait(...)，意思通常是：“你们几个一起跑，我盯着你们的状态，谁跑完了、谁还在跑，我要亲自处理。”

现在的 Python 3.11+ 还推出了更现代的 asyncio.TaskGroup，它比 gather 更安全（遇到异常会自动取消所有子任务）


# 3. 数据库中的“幂等”，就是指：无论这个操作执行 1 次还是执行 N 次，最终数据的结果都是一模一样的。

###  为什么数据库必须考虑幂等？（连接你之前的问题）
你之前说“我写的 sql 都没有用过重试”，但重试不一定是你的代码主动发的：

````
+ 网络抖动超时：你发了扣款请求，数据库执行成功了，但网络断了没返回结果。你的代码以为失败了，于是自动重试了第二次。如果没有幂等，用户会被扣两次钱。

+ 消息队列重复消费：Kafka/RabbitMQ 为了保证不丢消息，可能会重复投递同一条“加积分”指令。
````
幂等就是用来解决“重复请求”导致“数据错乱”的唯一解药。

### 数据库实现幂等的 3 种实战姿势
**姿势 A：依赖乐观锁（你刚学的 version）**

乐观锁本身就是天然的幂等保护机制。

````sql
UPDATE user_balance 
SET balance = balance - 10, version = version + 1 
WHERE user_id = 1 AND version = 5;
````
+ 第一次执行：匹配成功，影响行数 1。

+ 第二次执行（重试）：数据库里 version 已经变成 6 了，WHERE version = 5 匹配不到，影响行数 0。
结果： 只扣了一次钱。虽然第二次没做任何事，但最终数据保持一致。

**姿势 B：使用“幂等键”（最通用的方案**

在支付或下单场景，在数据库中建立一个唯一约束（Unique Key）。
````sql
-- 支付流水表
CREATE TABLE payment_log (
    id BIGINT PRIMARY KEY,
    order_id BIGINT,
    idempotent_key VARCHAR(64) UNIQUE, -- 唯一索引！
    amount DECIMAL,
    status VARCHAR(20)
);
````
**流程：**

1. 客户端发起支付时，生成一个全局唯一 ID（如 user_id + order_id + timestamp）作为 idempotent_key 传给后端。

2. 后端执行：
    ````sql
    INSERT INTO payment_log (idempotent_key, amount, status) 
    VALUES ('uuid_123', 100, 'SUCCESS');
    ````


# 4. 语言类型
| | 解释型（边跑边翻） | 编译型（全翻完再跑） |
| --- | --- | --- |
| **动态类型**（运行时看类型） | **Python / JavaScript / PHP**<br>（你最熟悉的模式：变量随便赋，运行时才发现类型传错了报错） | **Lisp / Julia / Scheme**<br>（极少见但存在，它们编译成机器码执行，但变量依然可以在运行时改变类型） |
| **静态类型**（写代码时定类型） | **Java（JVM刚启动时）/ C#（早期）**<br>（代码里写死 String s，虽然编译成字节码被解释执行，但类型在编译期就锁死了，运行时类型不会变） | **C / C++ / Rust / Go / Swift**<br>（最标准的模式：编译时类型锁死，运行时直接跑机器码） |


# 5. python2 和 python3的一些区别
🖨️ print 是语句还是函数？
这是最直观的区别。

+ Python 2: print 是一条语句，使用时可以不加括号。
    ````python
    print "Hello, World!"
    ````
+ Python 3: print() 是一个内置函数，必须使用括号。
    ````python
    Python 3: print() 是一个内置函数，必须使用括号。
    ````

🌐 字符串编码：ASCII 还是 Unicode？
这可以说是影响最大、最底层的改变。

+ Python 2: 默认使用 ASCII 编码。字符串有 str (字节串) 和 unicode 两种类型，处理中文等非英文字符时经常需要手动声明编码或进行转换，否则容易报错。

+ Python 3: 默认使用 UTF-8 编码。所有字符串都是 Unicode 字符串，原生支持中文等全球大部分语言的字符，无需额外处理。同时，新增了 bytes 类型用于处理原始的字节数据。

➗ 除法运算：结果是整数还是浮点数？
这会导致计算结果完全不同。

+ Python 2: 整数除法 / 的结果是整数（向下取整）。例如 3 / 2 的结果是 1。

+ Python 3: 除法 / 的结果总是浮点数。例如 3 / 2 的结果是 1.5。如果需要整数除法，统一使用 // 运算符。

🔄 函数返回值：列表还是迭代器？
Python 3 为了节省内存，将许多函数的返回类型从列表改为了更高效的迭代器或视图。

+ range() vs xrange(): Python 2 中 range() 返回列表，xrange() 返回迭代器；Python 3 中移除了 xrange()，range() 本身返回迭代器。

+ zip(), map(), filter(): Python 2 中这些函数返回列表；Python 3 中它们返回迭代器。

+ 字典方法 (keys(), values(), items()): Python 2 中这些方法返回列表；Python 3 中它们返回视图对象，更节省内存。

⌨️ 用户输入：raw_input() 还是 input()？
这关系到程序的安全和易用性。

+ Python 2: 有两个输入函数。raw_input() 将用户输入全部当作字符串处理；input() 则会尝试执行用户输入的内容，这是一个巨大的安全风险。

+ Python 3: 移除了 raw_input()，并将 input() 函数的行为统一为 Python 2 中 raw_input() 的行为，即总是返回字符串，安全且简单。

⚠️ 异常处理：语法更清晰
+ Python 2: 捕获异常时使用逗号分隔异常类型和实例。例如 except ValueError, e:。

+ Python 3: 强制使用 as 关键字来绑定异常实例，语法更清晰。例如 except ValueError as e:。

🏛️ 类：统一为新式类
+ Python 2: 存在“经典类”和“新式类”的区分。经典类是默认的，除非显式继承 object。

+ Python 3: 移除了经典类，所有类默认都是新式类，统一了面向对象模型，并更好地支持 super() 等高级特性。

### 其他值得关注的差异
+ True/False: 在 Python 2 中，True 和 False 是全局变量，可以被重新赋值；在 Python 3 中，它们是关键字，不能被修改。

+ has_key(): Python 2 中字典有 has_key() 方法；Python 3 中已废弃，统一使用 in 操作符，如 key in dict。

+ next(): Python 2 中迭代器有 next() 方法；Python 3 中统一使用内置函数 next(iterator)。

+ long 类型: Python 2 中有单独的 long 长整型；Python 3 中只有一种 int 类型，它能处理任意大小的整数。

# 6. Python3 中有六个标准的数据类型
| 6大分类 | 里面具体包含哪些类 | 是否可变 |
| --- | --- | --- |
| Number（数字） | int, float, bool, complex | 不可变 |
| String（字符串） | str | 不可变 |
| Tuple（元组） | tuple | 不可变 |
| List（列表） | list | 可变 |
| Dictionary（字典） | dict | 可变 |
| Set（集合） | set | 可变 |

# 7. uv教程
Astral 出品(同 Ruff),用 Rust 写的 Python 包管理器,一个工具替代 pip / pip-tools / virtualenv / pyenv / poetry / pipx,速度比传统工具快 10–100 倍。

## 7.1 管理 Python 本体
````shell
uv python install 3.12          # 安装某个版本(自带管理,不用 pyenv)
uv python list                  # 列出可用/已装版本
uv python pin 3.12              # 给当前项目固定一个 Python 版本(写入 .python-version)
uv 会自己下载并缓存各版本 Python,不污染系统。
````
## 7.2 项目工作流(最常用)
````shell
uv init my-app                  # 初始化项目(生成 pyproject.toml)
cd my-app

uv add requests                 # 添加依赖(自动建 venv、写锁文件)
uv add "fastapi>=0.100"         # 带版本约束
uv add --dev pytest             # 加开发依赖
uv add ruff --group lint        # 加到指定依赖组

uv remove requests              # 移除依赖

uv sync                         # 按 uv.lock 精确还原环境(队友/CI 必用)
uv lock                          # 只更新锁文件,不安装
uv run python main.py           # 在项目环境里跑命令(无需手动 activate)
````

### 7.2.1 空文件初始化
```` shell
uv init my-project        # 在当前位置新建并初始化 my-project/
# 或者进入文件夹后初始化:
cd my-project
uv init                   # 在当前目录初始化(而非新建子目录)
uv init 会生成:

pyproject.toml — 项目配置
.python-version — 固定 Python 版本
README.md、.gitignore
main.py(应用模板)
````

### 7.2.2 uv init 的默认选择
uv init,它不会随便用系统的旧版,而是优先用它已经管理的高版本。如果本机一个都没有,uv会自动下载一个合适的版本再用。

如何指定版本
````shell
# 方式一:init 时直接指定
uv init --python 3.12 my-app

# 方式二:已存在的项目改版本
uv python pin 3.12          # 改写 .python-version
# 同时建议同步改约束
# (手动编辑 pyproject.toml 的 requires-python)
uv sync                     # 重建环境
````

## 7.3 运行命令
````shell
uv run script.py                # 自动同步依赖后再运行
uv run pytest                   # 跑测试
uv run --with rich python -c "import rich; print('hi')"  # 临时加包,不污染项目
单文件脚本也能跑——uv 支持脚本内联声明依赖(Python 3.x 的 PEP 723):


# demo.py
# /// script
# dependencies = ["rich"]
# ///
import rich
rich.print("[bold green]Hello[/]")

uv run demo.py                  # 自动识别声明并装好 rich
````

## 7.4 临时工具(pipx 替代)
🤔 为什么需要 pipx 这样的工具？

通常我们用 pip 安装 Python 包，但这种方式安装命令行工具有两个主要问题：

+ 依赖冲突：用 pip 全局安装工具，可能会因为不同工具依赖的库版本不同，导致冲突。

+ 环境混乱：大量工具会逐渐“污染”全局Python环境，让你难以管理和清理。

pipx 的解决方案是为每个命令行工具创建一个独立的虚拟环境。这样，工具之间互不干扰，安装和卸载都很干净。安装后，pipx 会自动把工具的命令添加到系统的 PATH 中，让你能直接在终端里调用

```
uv tool 命令实现了 pipx 的核心功能，因此可以作为其直接替代品
```

````shell
uv tool run ruff check .        # 临时运行,不安装(类似 pipx run)
uv tool install ruff            # 全局安装命令行工具
uv tool list                    # 查看已装工具
uv tool upgrade ruff            # 升级
uv tool uninstall ruff
````

## 7.5 pip 兼容层(不想改工作流时)
````
你不需要把项目改成 pyproject.toml 格式，不需要学习 uv add，甚至连激活虚拟环境的步骤都不用变（uv pip 会自动识别你当前激活的 .venv）。你只是把发动机（底层引擎）从慢吞吞的旧 pip 换成了极速的 uv，但方向盘和油门踏板（命令行操作）完全没变。
````

uv 提供了一组镜像命令,语法和 pip 完全一致,只是更快:

````shell
uv pip install requests
uv pip install -r requirements.txt
uv pip freeze
uv pip compile requirements.in -o requirements.txt   # 生成锁文件
uv pip sync requirements.txt                          # 按锁文件同步
uv venv                       # 手动创建虚拟环境
````

## 7.6 常用速查
| 场景 | 命令 |
| --- | --- |
| 新建项目 | uv init |
| 加依赖 | uv add <pkg> |
| 还原环境 | uv sync |
| 跑代码 | uv run ... |
| 升级所有包 | uv lock --upgrade |
| 升级单个包 | uv lock --upgrade-package requests |
| 切换 Python | uv python pin 3.13 |
| 看包树/版本 | uv tree |

## 7.7 常见模式
克隆别人的项目后第一次跑:

````shell
git clone <repo> && cd <repo>
uv sync                  # 一键还原完整环境
uv run python main.py
````


# 8. encode和decode
```` html
   str (文本)  ──encode()──▶  bytes (字节)   存盘 / 走网络 / 传输
   给人看的字               给机器看的 0/1
              ◀──decode()──
````

# 9. 读文件操作的时候会使用 read、readline 或者 readlines，简述它们各自的作用。
````txt
read将整个文本都读取为一个字符串，占用内存大。
readline读取为一个生成器，支持遍历和迭代，占用空间小。
readlines将文本读取为列表，占用空间大。
````

# 10. Python 字典和 json 字符串相互转化方法
````
json.dumps()   将Python中的对象转换为JSON中的字符串对象
json.loads()   将JSON中的字符串对象转换为Python中的对象
````

# 11. 装饰器
装饰器本质上是一个接收函数作为参数、返回一个新函数的高阶函数。作用是在不修改原函数代码的前提下，给函数增加额外功能。

常见用途：

1. 日志记录
    
    记录函数调用时间、参数、返回值。

2. 权限校验

    比如 Web 开发里判断用户是否登录、是否有某个接口权限。

3. 性能计时

    统计函数执行耗时。

4. 缓存

    比如 functools.lru_cache，避免重复计算。

5. 重试机制

    函数失败时自动重试几次。

6. 路由注册

    Flask、FastAPI 里的 @app.get("/")，把函数注册成接口。

7. 输入校验

    在调用主逻辑前检查参数合法性

**demo**
````python
import time

def timer(func):
    def wrapper(*args, **kwargs):
        start = time.time()
        result = func(*args, **kwargs)
        end = time.time()
        print(f"{func.__name__} 耗时: {end - start:.4f}s")
        return result
    return wrapper

@timer
def slow_func():
    time.sleep(1)

slow_func()
加了 @timer 之后，slow_func 就被自动包装了一层计时逻辑，原函数完全不用改动。
````

# 12. uvicorn.run("app:app", host="0.0.0.0", port=8000, reload=True)
| 文件名   | 文件里的变量       | uvicorn 该写     |
|----------|--------------------|------------------|
| app.py   | app = FastAPI()    | "app:app"(你现在) |
| app.py   | api = FastAPI()    | "app:api"        |
| main.py  | app = FastAPI()    | "main:app"       |
| server.py| myapp = FastAPI()  | "server:myapp"   |
----
规律:左边写文件名,右边写 = FastAPI() 那行的变量名。

# 13. 如何判断一个对象是函数还是方法？
## 13.1 标准答案：用 inspect 模块精准判断 （type也可以的）
这是 Python 官方推荐的判断方式，也是代码里最严谨的写法。
````python
import inspect

def outside_func():
    pass

class MyClass:
    def instance_method(self):
        pass
    
    @classmethod
    def cls_method(cls):
        pass
    
    @staticmethod
    def static_method():
        pass

obj = MyClass()

# 1. 判断是否为普通函数（不绑定任何对象）
print(inspect.isfunction(outside_func))            # True
print(inspect.isfunction(MyClass.instance_method)) # True  ← 为什么是 True？
print(inspect.isfunction(MyClass.cls_method))     # False ← 为什么是 False？
print(inspect.isfunction(MyClass.static_method))  # True  ← 为什么是 True？
print(inspect.isfunction(obj.instance_method))    # False
print(inspect.isfunction(obj.cls_method))         # False
print(inspect.isfunction(obj.static_method))      # False
````
## 13.2 为什么 MyClass.instance_method 是 True？
````
一句话核心：普通函数（def 定义的）在通过类访问时，还没有和任何实例绑定，它依然是那个原始的 function 对象。
````
**底层机制拆解**
````
1. 你写 MyClass.instance_method。

2. Python 在 MyClass 的字典里找到了 instance_method，它的值是一个函数对象（类型是 <class 'function'>）。

3. 因为函数对象本身是描述符（实现了 __get__），Python 会调用 function.__get__(self, instance, owner)。

4. 关键点来了：此时 instance 参数是 None（因为没有实例对象参与）。

5. 根据 function.__get__ 的规则：如果 instance 是 None，直接返回原始函数对象本身（即 self），不做任何包装。

6. 所以你拿到的就是那个原始的 function，inspect.isfunction() 当然返回 True。
````
## 13.3 为什么 MyClass.static_method 是 True？
````
一句话核心：@staticmethod 装饰器的设计目的就是“取消绑定”，它在任何情况下（无论是通过类还是实例访问）都原样返回里面的原始函数。
````
**底层机制拆解**
````
1. 你写 MyClass.static_method。

2. @staticmethod 把原始函数包装成了一个 staticmethod 描述符对象。

3. Python 在 MyClass 的字典里找到了这个 staticmethod 对象，调用它的 __get__ 方法。

4. 关键点来了：staticmethod.__get__ 的逻辑是“铁憨憨”模式——它完全忽略 instance 和 owner 参数，直接把你定义时那个原始的函数对象原封不动地抛出来。

5. 所以你拿到的不是 staticmethod 对象本身，而是它里面包着的那颗原始 function 心脏。

6. inspect.isfunction() 作用于这个原始函数，返回 True。
````
## 13.4 为什么 MyClass.cls_method 是 False？（对比着看）
同样是通过类访问，但 @classmethod 的逻辑完全相反：
````
1. classmethod.__get__ 在 instance 为 None 时，不会偷懒返回原始函数。

2. 它会强制把类本身（owner）作为第一个参数塞进去，创建一个新的 method 对象返回给你。

3. 所以你拿到的不是一个原始函数，而是一个预先绑定了类参数的方法对象。

4. inspect.isfunction() 检查类型时发现它不是 function，所以返回 False。
````
## 13.5 为什么通过实例（obj.xxx）访问全都是 False？
无论你访问的是 obj.instance_method、obj.cls_method 还是 obj.static_method，只要通过实例去访问，Python 都会触发描述符的绑定协议，统统返回 method 对象（或绑定方法），而不是原始函数。
````
1. obj.instance_method：function.__get__ 发现 instance 是 obj，返回 Method 对象 → False。

2. obj.cls_method：classmethod.__get__ 发现 instance 是 obj，把类 MyClass 塞进去，返回 Method 对象 → False。

3. obj.static_method：虽然 staticmethod.__get__ 返回原始函数，但通过实例访问时，Python 的绑定机制会对最终结果再做一次“封装”，所以拿到的也不是纯粹的函数 → False。
````

## 13.6 一张表把全部结果彻底说死
| 代码 | 底层拿到的对象类型 | inspect.isfunction 结果 | 原因 |
| --- | --- | --- | --- |
| outside_func | function | True | 全局定义的普通函数，本身就是函数 |
| MyClass.instance_method | function（原始函数） | True | function.__get__ 无实例时返回自身 |
| MyClass.static_method | function（原始函数） | True | staticmethod.__get__ 直接解包返回原始函数 |
| MyClass.cls_method | method（绑定类的方法） | False | classmethod.__get__ 强制构建 method 对象 |
| obj.instance_method | method（绑定实例的方法） | False | 实例访问触发绑定，返回 method 对象 |
| obj.cls_method | method（绑定类的方法） | False | 实例访问依然触发绑定，返回 method 对象 |
| obj.static_method | method（包装后的方法对象） | False | 实例访问静态方法会额外封装一层 |

## 13.7 底层原理：从“描述符协议”讲起
为什么 Python 能实现这种“动态包装”？底层靠的就是描述符协议（Descriptor Protocol）。

描述符协议是 Python 控制属性访问的底层钩子。一个类只要实现了以下三个方法中的任意一个，它的实例就可以作为另一个类的类属性来拦截属性操作：
````
1. __get__(self, instance, owner)：读取属性时触发

2. __set__(self, instance, value)：赋值属性时触发

3. __delete__(self, instance)：删除属性时触发
核心结论：Python 里的函数（function 类）本身就是一个描述符！ 它实现了 __get__ 方法。

````
**函数__get__的伪代码逻辑**
````python
class Function:
    def __get__(self, instance, owner):
        if instance is None:
            # 情况1：通过类访问（MyClass.func），没有实例，返回原始函数
            return self
        else:
            # 情况2：通过实例访问（obj.func），有实例，返回包装好的"方法对象"
            return Method(self, instance)  # 把 instance 提前塞进去
````

这就完美解释了：

+ MyClass.instance_method：instance 是 None，返回原始函数 → isfunction 为 True。

+ obj.instance_method：instance 是 obj，返回包装的 Method 对象 → isfunction 为 False。

## 13.8 特殊装饰器的底层差异（@staticmethod vs @classmethod）
| 你在类里写 | 底层对应类型 | 它实现的 __get__ 逻辑 | 访问 MyClass.xxx 得到 |
| --- | --- | --- | --- |
| def func(self) | function | 无实例返回函数，有实例返回方法 | 原始函数 (True) |
| @staticmethod | staticmethod | 直接返回原始函数，啥也不包装 | 原始函数 (True) |
| @classmethod | classmethod | 强制把类（owner）作为第一个参数包装 | 方法对象 (False) |
----
+ @staticmethod 的巧妙：它利用描述符协议做了“偷懒”操作——直接把里面的函数原样抛出来，绕过了自动传 self 的机制。

+ @classmethod 的强制：它要求在类访问时就构建一个绑定到类自身的 method 对象。

# 14. 什么是描述符
描述符（Descriptor）是 Python 里一种控制属性访问行为的对象。

只要一个类实现了下面三个方法中的一个或多个，它的实例就是描述符：

```python
class MyDescriptor:
    def __get__(self, instance, owner):      # 获取属性时触发
        ...

    def __set__(self, instance, value):      # 设置属性时触发
        ...

    def __delete__(self, instance):          # 删除属性时触发
        ...
```
------
**一句话理解**
描述符 = 一个“拦截器”，当访问/设置/删除某个属性时，Python 会先问它要不要特殊处理。
------
**最简单的例子**
````python
# 不用描述符，属性访问就是普通的“取值/赋值”：
class Person:
    def __init__(self):
        self.age = 18

p = Person()
print(p.age)   # 18
p.age = 20     # 直接赋值

# 用了描述符，访问 age 时会被拦截：
class Validator:
    def __init__(self, min_value, max_value):
        self.min_value = min_value
        self.max_value = max_value

    def __set_name__(self, owner, name):
        self.name = name

    def __get__(self, instance, owner):
        if instance is None:
            return self
        return instance.__dict__.get(self.name)

    def __set__(self, instance, value):
        if not self.min_value <= value <= self.max_value:
            raise ValueError(f"年龄必须在 {self.min_value} 到 {self.max_value} 之间")
        instance.__dict__[self.name] = value


class Person:
    age = Validator(0, 150)   # age 是一个描述符对象

p = Person()
p.age = 18
print(p.age)    # 18

p.age = 200     # ❌ 报错：年龄必须在 0 到 150 之间
````
-----
## 14.1描述符的两种类型

| 类型 | 特点 | 例子 |
| --- | --- | --- |
| 非数据描述符 | 只实现 `__get__` | 函数对象（绑定方法就是这么来的） |
| 数据描述符 | 实现 `__set__` 或 `__delete__` | property、ORM 字段 |
数据描述符优先级更高：如果类属性是数据描述符，实例的__dict__里同名属性会被覆盖。
-----
## 14.2 函数为什么也是描述符？
````python
class Dog:
    def bark(self):
        print("汪汪")
这里的 bark 是一个函数对象，而函数对象内部实现了 __get__：


Dog.bark    # 通过类访问 → 返回函数本身
d.bark      # 通过实例访问 → 返回绑定方法（自动把 d 传给 self）
所以它本质上也是一个非数据描述符，负责把“函数”变成“方法”。
````
## 14.3 常见应用场景
+ 属性校验：上面的 Validator 例子。
+ 延迟加载：第一次访问时才去计算/读取数据。
+ 缓存：访问一次后把结果存起来，下次直接返回。
+ @property 装饰器：本质上就是描述符。
+ ORM 字段：比如 Django ORM 的 models.CharField()。




# 15. 什么是“实例字典”（__dict__）？
```
它本质上就是个普通字典（dict），只不过它是 Python 偷偷在后台给你创建的、专门用来存放对象属性值的一个字典。

```

在 Python 里，每一个对象（实例）都有一个自带的属性叫__dict__。你给对象赋值的所有变量，都存进这个字典里了：
````python
class MyClass:
    def __init__(self):
        self.name = "李四"  # 这个 name 会被存到实例字典里
        self.age = 20

obj = MyClass()
# 查看 obj 的"实例字典"
print(obj.__dict__)  # 输出：{'name': '李四', 'age': 20}
print(type(obj.__dict__))  # 输出：<class 'dict'> (看清楚，它就是普通的字典！)
````






# 16. Python 中的接口如何实现
## 16.1 鸭子类型（最 Pythonic，最松散）
````
Python 的哲学是：“如果它走起路来像鸭子，叫起来像鸭子，那它就是鸭子。”
不需要显式声明接口，只要对象有对应的方法就行：
````
````python
class Dog:
    def make_sound(self):
        print("汪汪")

class Cat:
    def make_sound(self):
        print("喵喵")

def animal_sound(a):   # 不 care 你是什么类，只要有 make_sound 就行
    a.make_sound()

animal_sound(Dog())    # 汪汪
animal_sound(Cat())    # 喵喵

````
这就是最松散的“接口”——约定大于定义。

## 16.2 抽象基类（ABC，最像 Java 的 interface）
用 abc 模块，可以强制子类必须实现某些方法：
````python
from abc import ABC, abstractmethod

class Animal(ABC):          # 抽象基类，相当于接口

    @abstractmethod
    def make_sound(self):
        pass

    @abstractmethod
    def run(self):
        pass


class Dog(Animal):

    def make_sound(self):
        print("汪汪")

    # 忘了实现 run() 会报错

if __name__ == "__main__":
    d = Dog()   # ❌ TypeError: Can't instantiate abstract class Dog 
                #    with abstract method run

````
**特点：**
| 特点 | 说明 |
| --- | --- |
| 不能实例化 | `Animal()` 会直接报错 |
| 子类必须实现所有抽象方法 | 否则也不能实例化 |
| 可以包含具体方法 | 这点比 Java 的 interface 更灵活 |
| 可以包含属性 | `@abstractmethod` + `@property` |

**抽象属性：**
````python
class Animal(ABC):

    @property
    @abstractmethod
    def name(self):
        pass
````

## 16.3 协议（Protocol，类型检查时代的接口）
Python 3.8+ 引入了 typing.Protocol，用于静态类型检查（比如 mypy）：
````python
from typing import Protocol

class Animal(Protocol):
    def make_sound(self) -> None: ...

class Dog:
    def make_sound(self) -> None:
        print("汪汪")

def animal_sound(a: Animal):
    a.make_sound()

animal_sound(Dog())   # ✅ 通过类型检查

----------------------------------------------
from typing import Protocol

class Flyable(Protocol):
    def fly(self) -> str:
        ...

class Bird:
    def fly(self) -> str:
        return "鸟在飞"

class Plane:
    def fly(self) -> str:
        return "飞机在飞"

class Dog:
    def run(self) -> str:
        return "狗在跑"

def take_off(obj: Flyable) -> None:
    print(obj.fly())

take_off(Bird())   # ✅ 通过类型检查
take_off(Plane())  # ✅ 通过类型检查
take_off(Dog())    # ❌ mypy 报错：Dog 没有 fly 方法
----------------------------------------------
from typing import Protocol

class Runnable(Protocol):
    def run(self) -> None:
        ...

class Dog:
    def run(self) -> None:
        print("狗跑")

def start(obj: Runnable) -> None:
    obj.run()

class Car:
    pass

start(Dog())   # ✅ 正常运行
start(Car())   # ⚠️ 运行时不会报错，但 mypy 会报错


````
**Protocol vs ABC 的区别：**
| | ABC | Protocol |
| --- | --- | --- |
| 运行时检查 | ✅ 会强制 | ❌ 不会 |
| 静态类型检查 | ✅ 可以 | ✅ 专门干这个 |
| 需要继承 | ✅ 子类要继承 | ❌ 不需要，Duck Typing 即可 |
| 更像 | Java interface | TypeScript interface |




# 17. 反射
````
程序在运行时，能够“检查、修改、调用”对象自身结构和行为的能力。

反射 = 运行时“照镜子” + “动手改”

getattr：拿
setattr：改
hasattr：查
delattr：删
dir / inspect：看
````

# 18. any和all
any() 和 all() 是 Python 内置的两个逻辑判断函数，它们接收一个可迭代对象（如列表、元组、生成器）作为参数，并返回一个布尔值。

**一句话定义区别：**

+ any(iterable)：“有一个是真就行”（逻辑或）。只要迭代器里有至少一个元素为 True，就返回 True；否则（全为假或空）返回 False。

+ all(iterable)：“全部是真才行”（逻辑与）。只有迭代器里所有元素都为 True，才返回 True；否则（有任意一个为假）返回 False。

⚠️ 特别注意（面试高频坑）

**空可迭代对象：**

+ any([]) 返回 False（因为“没有一个”是真的）。

+ all([]) 返回 True（因为“没有元素是假的”，这在数学逻辑上叫“虚真”/Vacuous truth）。

🧠 记忆口诀

+ any = “或”（OR）：any([a, b]) 等价于 a or b。

+ all = “与”（AND）：all([a, b]) 等价于 a and b。

# 19. 什么是猴子补丁（Monkey Patch）
````
猴子补丁指的是在程序运行时（runtime），动态地修改类或模块的属性/方法，
而不去改动它原始的源代码。

本质：利用 Python 的动态特性，把已有对象的属性“偷偷换掉”。
名字由来：英文 "guerrilla patch"（游击式打补丁）听起来像 "gorilla"，
后来又变成了 "monkey"，带有一点“非正规、打野”的调侃意味。
````

**最简单的例子：**
````python
# 原本的类
class Dog:
    def speak(self):
        return "汪汪"

# 运行时定义一个新方法
def new_speak(self):
    return "喵喵（被猴子补丁改了）"

# 动态替换掉原方法
Dog.speak = new_speak

d = Dog()
print(d.speak())   # 输出：喵喵（被猴子补丁改了）
````

````python
# 这是第三方库的代码（你无法修改）
# calculator.py
def add(a, b):
    return a - b  # 源代码写错了，应该返回 a + b

# ---------- 这是你的主程序 ----------
import calculator

# 你不需要去改 calculator.py，而是直接“猴子补丁”掉它
def new_add(a, b):
    return a + b  # 你写了一个正确的版本

calculator.add = new_add  # 关键一步：在运行时把函数替换掉！

# 测试
print(calculator.add(3, 5))  # 输出 8，而不是原来的 -2
````

**常见用途：**

| 场景 | 说明 |
| --- | --- |
| 测试 / Mock | 测试时把真实的网络请求、数据库调用替换成假的（如 `unittest.mock.patch`） |
| 第三方库打补丁 | 库有 bug 但又不想改源码 / 等不及官方修复，运行时临时修复 |
| 性能优化替换 | 经典案例：`import gevent.monkey; gevent.monkey.patch_all()`，把标准库的阻塞 IO 换成协程版 |
| 添加额外功能 | 给已有类动态加方法、加日志、加埋点 |

**优缺点：**

+ 优点：灵活，不改源码就能改行为，测试和救急非常方便。
+ 缺点：是“隐式魔法”，可读性和可维护性差。别人看源码以为是 A 行为，运行时却变成了 B，调试困难；多个补丁互相覆盖时容易冲突。

> 一句话总结：猴子补丁 = 运行时“偷梁换柱”。能用，但要克制，优先用在测试和救急场景。

# 20. Python 中的 is 和 == 有什么区别
````
is比较的是两个对象的id值是否相等，也就是比较两个对象是否为同一个实例对象，是否指向同一个内存地址。

==比较的是两个对象的内容是否相等，默认会调用对象的__eq__()方法。
````

# 21. lambda语法详解
````
lambda_expr ::=  "lambda" [parameter_list] ":" expression
````
+ ::= 表示“被定义为”；
+ "lambda" 是硬性关键字；
+ [parameter_list] 表示可选的参数列表（可以完全空着不写）；
+ ":" 是必须的分隔符；
+ expression 表示只能是一个表达式，绝不能是语句块。

**参数列表（parameter_list）的完整语法规则**
这是 lambda 最复杂的地方，它完全复刻了 def 函数定义时的参数规则，但写法上略有简化。你可以组合使用以下 5 种参数形式（按顺序）：

| 参数类型 | 语法示例 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| 1. 普通位置参数 | `lambda a, b, c:` | 调用时必须按位置传 | 最基础，必须传足个数。 |
| 2. 默认参数 | `lambda a, b=10:` | 调用时可省略 | `=` 是赋值表达式（此处合法），注意：默认值在定义时被计算（早绑定）。 |
| 3. 可变位置参数（*args） | `lambda *args:` | 调用时可传 0 或多个 | `*` 必须放在普通参数之后。注意：args 是一个元组。 |
| 4. 仅限关键字参数（Keyword-only） | `lambda *, key:` | 调用时必须 key=value | 必须放在 `*` 后面，或者放在 `*args` 后面。 |
| 5. 可变关键字参数（**kwargs） | `lambda **kwargs:` | 调用时可传 0 或多个 | `**` 必须放在最后。kwargs 是一个字典。 |
**合法的组合示例（极大体现了 Python 语法的灵活性）：**
````python
# 全组合：位置参数 + 默认参数 + *args + 仅关键字参数 + **kwargs
f = lambda a, b=2, *args, c, **kwargs: (a, b, args, c, kwargs)

# 调用验证
print(f(1, 3, 4, 5, c=6, x=7))  
# 输出: (1, 3, (4, 5), 6, {'x': 7})

------------------------------
# 不使用海象：计算了两次 abs
f1 = lambda x: abs(x) if abs(x) > 0 else 0

# 使用海象（赋值表达式）：只计算一次 abs，把结果存给变量 y
f2 = lambda x: (y := abs(x)) if y > 0 else 0

print(f2(-5))  # 输出 5
------------------------------


files = ["a.mp4", "b.avi", "c.mp4", "d.mov"]

# 一行流式处理（只处理 mp4，加上域名）
processed = list(map(lambda f: f"https://cdn.xx.com/{f}", filter(lambda f: f.endswith(".mp4"), files)))

print(processed)
# 输出： ['https://cdn.xx.com/a.mp4', 'https://cdn.xx.com/c.mp4']
````

**语法高压线（绝对不能写的）：**

❌ 不能有可变参数后再接普通位置参数（如 lambda *args, a: 除非 a 是仅关键字，否则 a 永远收不到值，语法允许但逻辑无效）。

❌ 不能有多个 *（如 lambda *a, *b:）语法报错。

❌ 默认参数不能放在无默认参数的前面（如 lambda a=1, b:）语法报错

**延迟绑定陷阱（面试必挂，架构必坑）**
需求：你想快速生成 3 个函数，分别乘以 0、1、2。这是循环里写 Lambda 最常见的翻车现场。
````python
# ❌ 翻车写法（你以为存了值，其实存了变量 i 的引用）
funcs_bad = [lambda x: x * i for i in range(3)]
print(funcs_bad[0](10))  # 期望 0，实际输出 20（因为循环结束 i=2，全部乘了 2）
print(funcs_bad[1](10))  # 期望 10，实际输出 20

# ✅ 正确写法（利用默认参数“冻住”当前 i 的值）
funcs_good = [lambda x, n=i: x * n for i in range(3)]
print(funcs_good[0](10))  # 输出 0
print(funcs_good[1](10))  # 输出 10
````
**运算符优先级的“大坑”（你一定会踩到）**
````python
# ❌ 错误写法（你以为返回元组，实际返回了 x）
bad = lambda x, y: x, y  
print(bad(1, 2))  
# 报错？ 不，它没报错！但输出是 1 （因为逗号把 lambda 截断了，它只返回了 x）

# ✅ 正确写法（必须加括号，确保表达式整体性）
good = lambda x, y: (x, y)  
print(good(1, 2))  
# 输出： (1, 2)
````

**列表排序 + 多字段提取（彻底玩转参数语法）**
需求：有一批待处理的视频任务，先按优先级降序，再按预估时长升序。
````python
tasks = [
    {"id": 1, "priority": 3, "time": 10},
    {"id": 2, "priority": 1, "time": 5},
    {"id": 3, "priority": 3, "time": 8}
]

# Lambda 返回元组 (优先级, 时长)，Python 会按元组顺序排序
# 注意：这里利用了 lambda 返回表达式的特性
tasks.sort(key=lambda t: (-t["priority"], t["time"]))

print(tasks)
# 输出： [{'id': 1, 'priority': 3, 'time': 10}, {'id': 3, 'priority': 3, 'time': 8}, {'id': 2, 'priority': 1, 'time': 5}]
````


# 22. dir() 是用来干什么的？
````
dir() 是 Python 内置函数，用来“查看一个对象身上有哪些属性和方法”，
返回的是一个按字母排序的字符串列表（属性名/方法名）。

它属于反射（自省）能力的一部分，常用来在不看文档的情况下，
快速摸清一个对象/模块到底能干什么。
````

**两种用法：**

| 用法 | 作用 |
| --- | --- |
| `dir()`（不传参数） | 返回当前作用域里所有的名字（变量、函数等） |
| `dir(obj)`（传对象） | 返回该对象所有可访问的属性和方法名 |

**示例：**
````python
# 1. 查看字符串对象有哪些方法
print(dir("hello"))
# 输出（节选）：['__add__', ..., 'upper', 'lower', 'split', 'strip', 'replace', ...]

# 2. 查看一个模块提供了什么
import math
print(dir(math))
# 输出（节选）：['pi', 'e', 'sqrt', 'ceil', 'floor', 'sin', ...]

# 3. 查看自定义类的实例
class Dog:
    def __init__(self):
        self.name = "旺财"
    def bark(self):
        return "汪汪"

print(dir(Dog()))
# 输出里能看到 'name'、'bark'，以及继承自 object 的一堆 __xxx__ 魔术方法
````

**常见用途：**

+ 调试 / 探索：拿到一个陌生对象，先 `dir()` 一下看它有啥。
+ 配合 `getattr` 做反射：先用 `dir()` 列出方法名，再用 `getattr(obj, name)` 动态调用。
+ 过滤魔术方法：`[m for m in dir(obj) if not m.startswith("__")]` 只看“正常”方法。

> 小贴士：`dir()` 只告诉你“有哪些名字”，想知道某个方法怎么用、是什么类型，还得配合 `help()`、`type()`、`inspect` 模块。底层上，`dir(obj)` 会去查对象的 `__dir__()` 方法。

# 23. 一个包里有三个模块，demo1.py、demo2.py 和 demo3.py，但使用 from tools import * 导入模块时，如何保证只有 demo1、demo3 被导入？

**要实现这个效果，核心秘籍就在包的 __init__.py 文件里。**
````
Python 的 from package import * 语法，会去检查该包（目录）下 __init__.py 文件中一个名为 __all__ 的列表变量。这个列表明确规定了“当别人用 * 导入时，我允许暴露哪些模块”。
````
针对该需求（只暴露 demo1 和 demo3，隐藏 demo2），操作步骤如下：

**文件目录结构**
````shell
project/
└── tools/                # 这是一个包（Package）
    ├── __init__.py       # 核心控制文件（必须存在）
    ├── demo1.py
    ├── demo2.py
    └── demo3.py
````
**修改 __init__.py（唯一的战场）**
````python
# tools/__init__.py

# 1. 【强烈推荐】显式导入需要暴露的模块，绑定到包命名空间
from . import demo1, demo3

# 2. 【核心关键】定义 __all__ 列表，限制 from tools import * 的行为
__all__ = ['demo1', 'demo3']
````

**验证效果**
````python
# 外部测试文件
from tools import *

print(demo1)  # 正常输出：<module 'tools.demo1' ...>
print(demo3)  # 正常输出：<module 'tools.demo3' ...>
print(demo2)  # 报错！NameError: name 'demo2' is not defined
````

**为什么这样设计?(底层逻辑深度解析)**
+ 为什么不能只写 __all__，不写 from . import？
    ````
    1. 如果你只写 __all__ = ['demo1', 'demo3']：
    from tools import * 依然只会导入 demo1 和 demo3（demo2 依然不可见）。
    但是，如果你后续还想用 import tools 然后调用 tools.demo1，这样写会报错，因为 demo1 没有被绑定到 tools 这个命名空间上。

    2. 加上 from . import demo1, demo3：
    好处是一劳永逸。无论外部是用 from tools import *、from tools import demo1 还是 import tools; tools.demo1，全部都能正常工作。这是 Python 官方推荐的最佳实践。
    ````
+ 如果 __init__.py 完全为空（不写任何代码），会发生什么？
    ````
    1. 执行 from tools import * 时，不会导入任何子模块（因为 __all__ 未定义，Python 默认拒绝导入子模块）。

    2. 外部必须显式写 from tools import demo1 才能使用。
    ````
+ demo2 会被其他模块看到吗？
    ````
    不会。demo2.py 文件依然存在，其他模块如果明确写 import tools.demo2 依然可以导入（因为 __all__ 只限制 *，不限制显式导入）。但在 from tools import * 的场景下，它对调用方完全不可见，实现了“隐藏内部实现细节”的效果。
    ````

# 24. 简述 w、a+ 和 wb 文件写入模式的区别。
| 模式 | 操作类型 | 文件存在时 | 文件不存在时 | 数据格式 | 是否可读 |
| --- | --- | --- | --- | --- | --- |
| w | 文本写入 | 清空原内容 | 创建新文件 | 字符串 (str) | 否 |
| a+ | 文本读写 | 保留原内容 (追加) | 创建新文件 | 字符串 (str) | 是 |
| wb | 二进制写入 | 清空原内容 | 创建新文件 | 字节流 (bytes) | 否 |
| ab | 二进制追加写入 | 保留原内容 (追加) | 创建新文件 | 字节流 (bytes) | 否 |
| ab+ | 二进制追加读写 | 保留原内容 (追加) | 创建新文件 | 字节流 (bytes) | 是 |
----

**规律总结：**
+ w 系列：都会清空原文件。
+ a 系列：都会保留原文件并追加。
+ 带 +：都增加了读取功能。
+ 带 b：数据格式都是字节流 (bytes)。

# 25. sort 和 sorted 的区别。
**核心区别**
+ list.sort()：是列表的方法。它会原地修改原列表，没有返回值（返回 None）。
+ sorted()：是 Python 的内置函数。它会保留原对象不变，并返回一个新的列表。

**代码示例对比**
````python
# 场景 A：使用 sort()
my_list = [3, 1, 4, 1, 5]
result = my_list.sort()

print(my_list)  # 输出: [1, 1, 3, 4, 5]  <-- 原列表被改变了
print(result)   # 输出: None             <-- 没有返回值

# 场景 B：使用 sorted()
my_list = [3, 1, 4, 1, 5]
result = sorted(my_list)

print(my_list)  # 输出: [3, 1, 4, 1, 5]  <-- 原列表保持不变
print(result)   # 输出: [1, 1, 3, 4, 5]  <-- 返回了一个新列表

# 1. 排序元组 (Tuple)
my_tuple = (3, 1, 2)
print(sorted(my_tuple))  # 输出: [1, 2, 3] (注意：返回的是列表)

# 2. 排序字典 (Dictionary) -> 默认对键 (Key) 排序
my_dict = {'banana': 3, 'apple': 1, 'cherry': 2}
print(sorted(my_dict))   # 输出: ['apple', 'banana', 'cherry']

# 3. 排序字符串 (String)
my_str = "python"
print(sorted(my_str))    # 输出: ['h', 'n', 'o', 'p', 't', 'y']
````
**详细对比表**

| 特性 | `list.sort()` | `sorted()` |
| --- | --- | --- |
| 类型 | 列表（List）的方法 | 全局内置函数 |
| 是否修改原对象 | 是（原地修改） | 否（保留原样） |
| 返回值 | None | 一个新的已排序的列表 |
| 适用对象 | 只能是列表 | 任何可迭代对象（列表、元组、字典、字符串、集合等） |

# 26. 讲讲 Python 中的位运算符。
**假设变量初始值为：**
+ a = 60 （二进制：0011 1100）
+ b = 13 （二进制：0000 1101）
### 按位运算符速查表

| 运算符 | 名称 | 运算规则（口诀） | 示例结果 | 二进制解析 (8位) |
| --- | --- | --- | --- | --- |
| `&` | 按位与 | 全 1 为 1，否则为 0 | 12 | 0000 1100 |
| `\|` | 按位或 | 有 1 为 1，全 0 为 0 | 61 | 0011 1101 |
| `^` | 按位异或 | 相同为 0，相异为 1 | 49 | 0011 0001 |
| `~` | 按位取反 | 1 变 0，0 变 1 | -61 | 1100 0011 (补码) |
| `<<` | 左移 | 高位丢弃，低位补 0 | 240 | 1111 0000 |
| `>>` | 右移 | 低位丢弃，高位补符号位 | 15 | 0000 1111 |
----
### 核心原理解析
**1. 逻辑运算 (&, |, ^)**
+ 按位与 (&)：常用于清零或提取特定位。例如 a & 1 可以判断奇偶。
+ 按位或 (|)：常用于位置 1。例如将某个特定位置设为 1。
+ 按位异或 (^)：常用于翻转特定位或无临时变量交换数值（a = a ^ b; b = a ^ b; a = a ^ b）。

**2. 按位取反 (~)**
+ 数学公式：在计算机中，~x 等价于 -(x + 1)。所以 ~60 = -61。
+ 补码解释：正数的补码是其本身，负数的补码是“取反加 1”。60 取反得到 1100 0011，在 8 位有符号二进制中，最高位 1 代表负数，其值即为 -61。

**3.移位运算 (<<, >>)**
+ 左移 (<<)：a << n 相当于 a * (2^n)。
    + 示例：60 << 2 = 60 * 4 = 240。
+ 右移 (>>)：a >> n 相当于 a // (2^n) (整除)。
    + 示例：60 >> 2 = 60 // 4 = 15。
    + 注意：对于负数右移，高位会补 1（算术右移），以保持其负数性质。

# 27. 补码详解
````
补码（Two's Complement）是计算机存储和计算有符号整数（正数、负数、零）的统一编码方案。
--它让 CPU 只用一套加法电路，就能搞定加法和减法，极大地简化了硬件设计。
````
### 为什么需要补码？（核心痛点）
我们很容易理解"正负号"（+5 和 -5），但计算机只有晶体管，只能识别 0 和 1。

+ 方案一：符号位表示法（最高位 0 正 1 负）。这会导致 +0（0000）和 -0（1000）同时存在，且加法电路需要额外设计减法器，极其麻烦。

+ 补码方案：把负数映射到正数空间的"镜像"位置上，让 加法器 可以通过溢出（模运算）自然得到减法结果。解决了±0的问题，且减法变加法。

### 补码的铁律（数学定义）
**对于 N 位（比如 8 位、32 位）的二进制：**

+ 正数（包括 0）：直接转换为普通二进制（最高位必须是 0）。

+ 负数（-x）：将 x 对应的正数取反，再加 1。即 [-x]_补 = (~x) + 1。

**运算公式（恒成立）：**[X]_补 + [-X]_补 = 2^N（刚好溢出 N 位，留下的 0 就是结果）。

**手动推演（8 位示例，关键要盯着每一步的变化）：**


| 十进制 | 原码（直观） | 补码（计算机存储） | 推导过程 |
| --- | --- | --- | --- |
| +1 | 0000 0001 | 0000 0001 | 正数，直接存 |
| -1 | 1000 0001 | 1111 1111 | 取反 0000 0001 → 1111 1110，再 +1 = 1111 1111 |
| +2 | 0000 0010 | 0000 0010 | 正数，直接存 |
| -2 | 1000 0010 | 1111 1110 | 取反 0000 0010 → 1111 1101，再 +1 = 1111 1110 |
| 0 | 0000 0000 | 0000 0000 | 正数，直接存（-0 补码？ 1111 1111 + 1 溢出变 0000 0000） |
----
**关键规律：** 负数补码的最高位永远是 1。-1 在补码里是全 1（1111 1111），这就是为什么 ~0 = -1 的底层根源。

### 补码的范围（N 位死锁）
N 位补码能表示的数字范围是：-2^(N-1) 到 2^(N-1) - 1。

+ 8 位：-128 ~ 127。

+ 32 位：-2147483648 ~ 2147483647。

**对称性的缺失：** 负数的绝对值比正数多一个（-128 没有对应的 +128）。
因为 1000 0000（-128）取反再加 1 会变回自身（0111 1111 + 1 = 1000 0000），这是一个死循环，用于表示最小负数。

### CPU 视角：加法器如何做减法？（补码的高光时刻）
CPU 没有减法指令（只有加法器）。计算 5 - 3，在 CPU 眼里是 5 + (-3)。

+ 5 补码：0000 0101

+ -3 补码：1111 1101

+ 相加：0000 0101 + 1111 1101 = 1 0000 0010（溢出了 1 位，丢弃最高位），剩下 0000 0010（就是十进制 2）。完美！

### 实战中的经典陷阱（补码导致的 Bug）
+ 右移（>>）算术移位陷阱：-1 >> 1 结果还是 -1（因为补码全 1 右移，高位补 1 保持负数）。如果你误以为这是除以 2，就会死循环。

+ 颜色/图像处理（RGB 转码）：在做 pixel = ~pixel 增强图片负片时，如果不是用 & 0xFF 截断，Python 会给你返回负数，导致写入文件时抛出 OverflowError。



# 28. Python 中会有函数或成员变量包含单下划线前缀和结尾，或双下划线前缀结尾，它们的区别是什么？
````
在 Python 中，下划线的使用有着非常严格的命名约定（Naming Conventions）。Python 并没有像 Java 或 C++ 那样真正的 private 或 public 关键字，而是通过下划线来向开发者传递“访问权限”的信号。
````

### 单下划线前缀：_var （弱私有 / 内部使用）
+ **含义：** 这是一个约定俗成的规范，表示该变量或方法是类的内部实现细节，或者是模块的私有成员。
作用：
+ **对人：** 告诉其他程序员“这是内部用的，请不要在外部直接调用或修改”。
+ **对机器：** 在使用 from module import * 导入模块时，不会导入以单下划线开头的名称。
示例：
````python
class MyClass:
    def __init__(self):
        self._internal_var = 10  # 外部可以访问，但不建议

    def _internal_method(self):  # 内部方法
        pass
````

### 单下划线后缀：var_ （避免关键字冲突）
+ **含义：** 用于避免与 Python 内置关键字或保留字发生命名冲突。
+ **作用：** 当你想用一个变量名，但它恰好是 Python 的关键字（如 class, type, list）时，在末尾加一个下划线。
+ **示例：**
````python
# class 是关键字，不能用作变量名
Tkinter.Tk().mainloop(class_='my_window') 

# 或者在函数参数中
def send_message(to, from_, content):
    pass
````

## 双下划线前缀：__var （名称修饰 / 强私有）
+ **含义：** 这会触发 Python 的名称修饰（Name Mangling） 机制。它的主要目的不是为了防止外部访问，而是为了防止在继承时子类与父类的属性发生命名冲突。
+ **作用：** Python 解释器在底层会自动将 __var 重命名为 _ClassName__var。
+ **示例：**
````python
class Parent:
    def __init__(self):
        self.__secret = 100  # 会被重命名为 _Parent__secret

class Child(Parent):
    def __init__(self):
        super().__init__()
        self.__secret = 200  # 会被重命名为 _Child__secret，不会覆盖父类的

p = Parent()
# print(p.__secret)  # 报错：AttributeError
print(p._Parent__secret)  # 输出 100（依然可以强行访问，所以不是绝对安全）
````

## 双下划线前后缀：__var__ （魔术方法 / 特殊方法）
+ **含义：** 这是 Python 内部保留的命名，通常被称为“魔术方法”（Magic Methods）或“双下方法”（Dunder Methods）。
+ **作用：** 用于实现 Python 的特定协议或内置功能（如初始化、字符串表示、运算符重载等）。
+ **警告：** 绝对不要在自己的代码中自定义这种格式的变量名，以免与未来 Python 版本的内置功能冲突。
+ **示例：**
````python
class MyClass:
    def __init__(self):  # 构造函数
        pass
        
    def __str__(self):   # 定义 print() 时的输出
        return "MyClass instance"
        
    def __len__(self):   # 定义 len() 的行为
        return 5
````
## 单独一个下划线：_ （占位符 / 临时变量）
+ **含义：** 通常用作一次性变量或占位符，表示“我不关心这个值”。
+ **常见场景：**
    
    **在循环中：** 表示循环次数，但不使用索引
    ````python
    for _ in range(5):
        print("Hello")
    ````
    **在解包（Unpacking）时：** 忽略不需要的值。
    ````python
    x, _, y = (1, 2, 3)  # 忽略中间的 2
    ````

##  总结速查表

| 命名模式 | 名称 | 核心含义 | 访问限制 |
| --- | --- | --- | --- |
| `_var` | 单下划线前缀 | 内部使用 / 弱私有 | 约定俗成不访问；import * 不导入 |
| `var_` | 单下划线后缀 | 避免关键字冲突 | 无限制，纯粹为了命名合法 |
| `__var` | 双下划线前缀 | 名称修饰 / 防继承冲突 | 解释器自动改名为 `_Class__var` |
| `__var__` | 双下划线前后缀 | Python 魔术方法 | 系统保留，用户切勿自定义 |
| `_` | 单下划线 | 占位符 / 忽略值 | 无实际逻辑意义，仅提高代码可读性 |

# 29. 什么是面向对象的 MRO？
````
在面向对象编程（特别是 Python）中，MRO 的全称是 Method Resolution Order，中文翻译为方法解析顺序。

简单来说，MRO 决定了当你在一个类中调用某个方法或属性时，Python 应该按照什么顺序去查找它。
````

## 为什么需要 MRO？
在**单继承**中，查找顺序非常直观：
````
子类 →→ 父类 →→ 父类的父类 →→ ... →→ object
````
但在**多重继承**（一个子类继承多个父类）中，情况就会变得复杂。如果多个父类中有同名方法，Python 该先调用谁的？如果继承关系像菱形一样交叉（菱形继承），又该如何避免重复查找或死循环？

**MRO 就是为了解决多重继承中的方法查找顺序问题而制定的一套规则。**

## Python 中的 MRO 算法：C3 线性化
在早期的 Python 2 中，使用的是简单的“深度优先搜索”，但这在处理“菱形继承”时会出错。

从 Python 2.3 开始（包括现在的 Python 3），Python 采用了 C3 线性化算法（C3 Linearization） 来计算 MRO。

**C3 算法遵循三个核心原则：**

+ **子类优先：** 子类的方法永远比父类的方法先被找到。
+ **从左到右：** 在多重继承 class Child(Parent1, Parent2) 中，Parent1 中的方法优先于 Parent2。
+ **单调性：** 如果在子类中已经确定了某些类的顺序，那么在更下层的子类中，这个顺序不能被打乱。

## 如何查看 MRO？
在 Python 中，你可以通过以下两种方式查看任何类的 MRO 顺序：
+ 使用 __mro__ 属性（返回一个元组）。
+ 使用 mro() 方法（返回一个列表）。
````python
class A: pass
class B(A): pass
class C(A): pass
class D(B, C): pass

# 查看 D 类的 MRO
print(D.__mro__)
# 输出: (<class '__main__.D'>, <class '__main__.B'>, <class '__main__.C'>, <class '__main__.A'>, <class 'object'>)

print(D.mro())
# 输出: [<class '__main__.D'>, <class '__main__.B'>, <class '__main__.C'>, <class '__main__.A'>, <class 'object'>]
````
**解读：** 当调用 D 的实例方法时，Python 会按照 D -> B -> C -> A -> object 的顺序依次查找。

## 经典案例：菱形继承（Diamond Problem）
这是最能体现 MRO 作用的场景。
````python
class A:
    def show(self):
        print("A")

class B(A):
    def show(self):
        print("B")

class C(A):
    def show(self):
        print("C")

class D(B, C):
    pass

d = D()
d.show()  # 输出: B
````
**查找过程分析：**
+ 调用 d.show()，Python 查看 D 的 MRO：D -> B -> C -> A -> object。
+ 在 D 中没找到 show。
+ 在 B 中找到了 show，立即执行并停止查找。
+ 结果输出 B。

如果没有 MRO（比如使用旧版的深度优先搜索），顺序可能会变成 D -> B -> A -> C -> A，这会导致 A 被查找两次，甚至在调用 super() 时引发逻辑错误。C3 算法完美地保证了 A 只会在 B 和 C 之后被查找一次。

## 总结
+ MRO 是什么：方法解析顺序，决定了多重继承下属性和方法的查找路径。
+ 核心算法：Python 3 使用 C3 线性化算法。
+ 主要作用：解决多重继承（尤其是菱形继承）中的方法冲突，确保查找顺序唯一、合理且不重复。
+ 记忆口诀：“从左到右，深度优先，但保证共同父类最后找”。

# 30. python的垃圾回收机制
````
Python 的垃圾回收机制（Garbage Collection，简称 GC）是 Python 自动管理内存的核心功能，它的主要任务是自动找出并释放那些不再被使用的内存，防止内存泄漏。
````
**Python（特指 CPython 解释器）的垃圾回收机制并不是单一的，而是由三套机制组合而成的：**
+ 引用计数（主力军）
+ 标记-清除（解决循环引用）
+ 分代回收（优化回收效率）

### 引用计数（Reference Counting）—— 主力军
这是 Python 最基础、最核心的垃圾回收机制。

+ **原理：** Python 中的每个对象都有一个“引用计数器”。
    + 当有一个新的变量指向这个对象时，计数器 +1。
    + 当变量被删除、重新赋值或超出作用域时，计数器 -1。
    + **当计数器变为 0 时，说明没有任何人需要这个对象了，Python 会立刻回收它的内存。**

+ **代码示例：**
    ````python
    a = [1, 2, 3]  # 列表对象的引用计数为 1
    b = a          # 引用计数变为 2
    del a          # 引用计数变为 1
    b = None       # 引用计数变为 0，列表对象被立刻回收
    ````
+ **优点：** 实时性极高，一旦没用了马上回收，内存管理开销小。
+ **致命缺点：** 无法解决“循环引用”问题。

+ **循环引用问题**
````python
list1 = []
list2 = []
list1.append(list2)  # list1 引用了 list2
list2.append(list1)  # list2 引用了 list1
del list1
del list2
````
此时，虽然外部没有变量指向这两个列表了，但它们**互相引用**，导致彼此的引用计数都是 1，永远不会变成 0。这就造成了**内存泄漏**。

### 标记-清除（Mark and Sweep）—— 救火队员

为了解决上面“循环引用”导致的内存泄漏，Python 引入了“标记-清除”机制。它专门用来处理容器对象（如列表、字典、自定义类实例等，因为它们才可能产生循环引用）。

它的工作分为两步：

+ **标记（Mark）**：
    + Python 会从“根节点”（比如全局变量、当前正在执行的函数局部变量、栈帧等）出发，顺着引用链往下找。
    + 所有能被“根节点”直接或间接访问到的对象，都被标记为 “存活（Reachable）”。
+ **清除（Sweep）**：
    + 遍历所有容器对象，没有被标记为“存活”的对象，就是垃圾（包括那些互相循环引用但外部已经无法访问的对象）。
    + 将这些垃圾对象统一清除，回收内存。


### 分代回收（Generational Collection）—— 效率优化器

“标记-清除”虽然解决了循环引用，但每次都要遍历所有对象，非常耗时。为了提高效率，Python 引入了“分代回收”机制。

它基于一个统计学上的 **“弱代假说”（Weak Generational Hypothesis）** ：
````
“大部分对象都是朝生夕死的（刚创建很快就没用了），而存活时间越长的对象，越不容易变成垃圾。”
````
基于这个理论，Python 将内存中的对象分为三代（0代、1代、2代）：
+ 0代（新生代）：所有新创建的对象都放在 0 代。
+ 1代（中年代）：0 代对象经历了一次垃圾回收后还活着的，晋升到 1 代。
+ 2代（老年代）：1 代对象经历了一次垃圾回收后还活着的，晋升到 2 代。

**回收策略（阈值触发）**：

Python 为每一代设定了阈值（默认：0代700次，1代10次，2代10次）。
+ 当 0 代对象数量达到 700 个时，触发 0 代 GC。
+ 当 1 代被扫描的次数达到 10 次时，触发 1 代 GC（同时也会扫描 0 代）。
+ 当 2 代被扫描的次数达到 10 次时，触发 2 代 GC（全面扫描 0、1、2 代）。

**结论：** 新对象（0代）被频繁检查，老对象（2代）很少被检查。这样大大减少了 GC 的扫描范围，提升了程序运行效率。

````
垃圾回收能回收所有资源吗？
不能！ 垃圾回收只能回收内存。对于文件句柄、网络连接、数据库连接等系统资源，必须通过 with 语句或手动调用 .close() 来释放，不能依赖 GC。
````

# 31. 打开浏览器访问 www.baidu.com 获取到结果，简述整个流程。
````
在浏览器地址栏输入 www.baidu.com 并回车后，整个过程可以分为网络通信和浏览器渲染两大阶段。以下是完整的流程简述：
````

### 第一阶段：网络通信（获取数据）
1.**URL 解析与缓存检查**
+ **解析 URL**： 浏览器首先解析你输入的 URL，确认协议（HTTP/HTTPS）、域名（www.baidu.com）和路径（/）。
+ **检查缓存**：
    + 浏览器会按顺序检查是否有该域名的 IP 缓存：
        ````
        浏览器缓存 →→ 操作系统缓存（hosts 文件） →→ 路由器缓存 →→ ISP（网络服务提供商）DNS 缓存。
        ````
    + 如果缓存中有且未过期，直接跳到第 3 步；如果没有，则进入第 2 步。
    
2.**DNS 解析（域名 →→ IP 地址）**
+ 浏览器向本地 DNS 服务器发起请求，查询 www.baidu.com 的 IP 地址。
+ 如果本地 DNS 没有，它会依次向根域名服务器 →→ 顶级域名服务器（.com） →→ 权威域名服务器（baidu.com） 发起递归或迭代查询。
最终获取到百度服务器的真实 IP 地址（例如 180.101.50.241），并将其缓存。

3.**建立 TCP 连接（三次握手）**
+ 浏览器拿到 IP 后，根据协议确定端口（HTTP 是 80，HTTPS 是 443）。
+ 浏览器与服务器发起 TCP 三次握手，建立可靠的传输通道：
    + 客户端发送 SYN 报文（请求连接）。
    + 服务端回复 SYN+ACK 报文（同意连接）。
    + 客户端回复 ACK 报文（确认连接）。

4.**TLS/SSL 握手（如果是 HTTPS）**
+ 注：现在输入 www.baidu.com，浏览器会自动跳转到 https://，所以这一步必不可少。
+ 客户端和服务端通过 TLS 协议协商加密算法、验证服务器证书（防止中间人攻击），并生成用于后续通信的对称加密密钥。

5.**发送 HTTP 请求**

+ 浏览器构建 HTTP 请求报文，通过 TCP 连接发送给服务器。报文包含：
    + 请求行：GET / HTTP/1.1（请求方法、路径、协议版本）。
    + 请求头：Host: www.baidu.com、User-Agent（浏览器信息）、Cookie 等。
    + 请求体：GET 请求通常为空。

6.**服务器处理并返回响应**

+ 百度的服务器（如 Nginx）接收到请求，经过负载均衡、后端逻辑处理（可能查询数据库等），生成 HTML 文档。
+ 服务器返回 HTTP 响应报文：
    + 状态行：HTTP/1.1 200 OK（表示请求成功）。
    + 响应头：Content-Type: text/html（数据类型）、Content-Length、Set-Cookie 等。
    + 响应体：具体的 HTML 代码。


## 第二阶段：浏览器渲染（展示页面）
7.**浏览器解析与渲染**

浏览器拿到 HTML 数据后，开始渲染页面，过程如下：
+ 解析 HTML：逐行读取 HTML，构建 DOM 树（文档对象模型）。
+ 解析 CSS：解析外部 CSS 文件和内联样式，构建 CSSOM 树（CSS 对象模型）。
+ 生成渲染树：将 DOM 树和 CSSOM 树合并，生成 Render Tree（渲染树，只包含可见节点）。
+ 布局（Layout/Reflow）：计算渲染树中每个节点在屏幕上的确切位置和大小。
+ 绘制（Paint）：将节点实际绘制到屏幕上。
+ 合成（Composite）：对于使用了 CSS3 动画或 z-index 的层，进行图层合成，最终显示在屏幕上。
+ ```注意：在解析 HTML 时，如果遇到 <script> 标签，会暂停 DOM 解析去下载并执行 JS；遇到 CSS 会阻塞渲染但不会阻塞 DOM 解析。```

8.**断开连接（TCP 四次挥手）**
+ 如果 HTTP 响应头中包含 Connection: close，或者页面所有资源加载完毕且连接空闲超时，浏览器会发起 TCP 四次挥手断开连接：
+ 客户端发送 FIN（请求断开）。
+ 服务端回复 ACK（确认收到）。
+ 服务端发送 FIN（同意断开）。
+ 客户端回复 ACK（确认断开，进入 Time-Wait 状态后彻底关闭）。
+ ```注：现代 HTTP/1.1 默认开启 keep-alive，连接会保持一段时间以便复用。```

# 32. 什么是CDN缓存
CDN缓存，是一张部署在全球的、巨大的分布式“网络副本网”。

它的核心思想是“就近复制，快速交付”，通过在离你最近的地方存储网站内容的副本，让你能飞速访问，而无需每次都去遥远的源服务器“取货”。

要理解CDN缓存，可以把它想成一个全球连锁的“本地仓库”网络：

+ 源站 (Origin Server)：就像是总工厂，所有商品的“正版”都存放在这里。

+ CDN节点 (Edge Node)：就像是分布在城市各处的本地仓库（边缘节点），里面存放着从总工厂运来的热门商品副本。

+ 用户 (End User)：就像是消费者，购物时，系统会自动引导你去最近的仓库提货，而不是总工厂。

````这个“本地仓库”网络由CDN服务商（如Cloudflare、Akamai、阿里云等）在世界各地部署。当用户访问一个使用了CDN的网站时，请求会被智能地引导到离他地理位置最近的CDN节点。````

### 它是怎么工作的？
整个工作过程可以简化为两个核心步骤：

+ 智能调度：当你访问一个网站，CDN的智能调度系统会根据你的IP地址、网络状况和各节点的负载情况，快速分配一个离你最近的CDN节点。

+ 缓存命中与回源：

    + 缓存命中 (Cache Hit)：如果这个最近的CDN节点上恰好有你要的内容，它会直接返回给你，响应极快（通常<50ms）。

    + 缓存未命中 (Cache Miss)：如果节点上没有，它会作为“代购”，去源站服务器取一份内容。拿到后，不仅会返回给你，还会自己存一份副本，方便下一位用户。

````
我们自己搭建的Web服务器（比如用Nginx、Apache、Tomcat搭建的）本身并不自带CDN缓存。它只是一个“内容源站”，负责存储和生成最原始的数据。
````

# 33. DNS劫持
````
DNS（域名系统）就像互联网的“电话簿”。我们记不住复杂的IP地址（如192.168.1.1），只记得域名（如www.example.com）。DNS服务器的任务，就是帮我们把域名“翻译”成对应的IP地址，从而找到正确的服务器。

DNS劫持，就是攻击者用各种手段，篡改了这个“翻译”过程，让你在输入正确域名后，被引向一个错误的、由攻击者控制的恶意IP地址。
````
### 常见的劫持方式
```攻击者会在DNS查询链的不同环节下手，主要有以下几种方式：```

+ **本地劫持**：攻击最直接的一层。通过在你的电脑或手机上植入恶意软件，直接修改本地的“小电话簿”（hosts文件）或DNS设置。这样，只有你的设备会被影响。

+ **路由器劫持**：这是影响最广的方式之一。很多家用路由器存在漏洞或使用默认密码。攻击者破解后，篡改路由器的DNS设置，整个家庭或办公室网络中的所有设备都会被一网打尽。据安全机构监测，超过30%的企业网络曾遭遇此类事件。2026年6月，国家互联网应急中心（CNCERT）就曾警告，国内有单日超过70万个独立IP因家用路由器DNS被篡改而受影响。

+ **中间人攻击**：攻击者在你的设备和DNS服务器之间的通信线路上“潜伏”下来。他们拦截你的DNS查询请求，然后伪造一个假的回复给你。这种攻击非常隐蔽，你的设备配置看起来一切正常。

+ **DNS缓存投毒**：攻击者利用DNS协议的漏洞，向公共DNS服务器（如你的网络运营商提供的DNS）注入虚假的解析记录。一旦成功，所有使用该DNS服务器的用户都会被影响。

+ **权威服务器入侵**：这是最高级别的劫持。攻击者直接攻破域名注册商或DNS托管平台，篡改你域名的官方解析记录。这会导致你整个网站的访问都被劫持，影响范围极广。

### 被劫持的后果

```DNS劫持的后果远不止“打不开网页”这么简单：```

+ **钓鱼诈骗**：被引导至高仿的银行、电商网站，导致账号、密码、银行卡信息泄露。

+ **广告牟利**：被强制插入各种烦人的广告，攻击者以此获利。

+ **传播恶意软件**：访问的网站可能自动下载病毒、勒索软件，使你的设备被控制。

+ **服务中断**：你的网站无法被正常访问，导致业务停滞和客户流失。


# 34. TCP 和 UDP 的区别有哪些？
````
TCP是面向连接、保证数据完整有序到达的可靠传输协议，但开销大、速度慢；UDP则是无连接、不保证送达和顺序的不可靠传输协议，但开销小、速度快。

     1. TCP协议在传送数据段的时候要给段标号；UDP协议不
     2. TCP协议可靠；UDP协议不可靠
     3. TCP协议是面向连接；UDP协议采用无连接
     4. TCP协议负载较高，采用虚电路；UDP采用无连接
     5. TCP协议的发送方要确认接收方是否收到数据段（3次握手协议）
     6. TCP协议采用窗口技术和流控制
````

```TCP和UDP是互联网传输中最核心的两种协议。要理解它们的区别，可以用一个非常生活化的类比：TCP就像打电话，UDP就像寄明信片。```

+ **打电话（TCP）**：必须先拨号、对方接通（建立连接），才能开始说话。你问一句，对方答一句（确认收到），如果没听清，你会要求对方重复（重传）。整个过程可靠、有序，但通话期间占着线路。

+ **寄明信片（UDP）**：不需要提前打招呼，直接把明信片扔进邮筒就完事了。你无法确认对方是否收到，寄丢了的明信片也不会补发。但这种方式极其轻便，邮筒里一次能塞成千上万张。

### 核心区别对比表
| 对比维度 | TCP (传输控制协议) | UDP (用户数据报协议) |
| --- | --- | --- |
| 连接状态 | 面向连接：传输前必须通过“三次握手”建立连接。 | 无连接：发送数据前无需任何准备，直接发。 |
| 可靠性 | 高度可靠：有确认机制（ACK），丢包会自动重传，确保数据完整到达。 | “尽力而为”：不保证送达，丢包了就丢了，不会重传。 |
| 数据顺序 | 保证有序：数据包有编号，接收方会按编号重组，不会乱序。 | 不保证顺序：先发的包可能后到，接收方无法保证顺序。 |
| 速度与开销 | 较慢，开销大：因为要维护连接、确认、重传，头部信息较大（20字节）。 | 极快，开销小：简单粗暴，头部仅8字节，没有复杂的校验机制。 |
| 拥塞控制 | 有：会根据网络状况主动调整发送速率，避免压垮网络。 | 无：不管网络堵不堵，只管一股脑发，可能造成网络堵塞。 |
| 流量控制 | 有：会协调发送方和接收方的速率，防止接收方处理不过来。 | 无：接收方处理不过来时，数据就直接丢弃。 |
----
### 各自的应用场景
正因为一个“稳”一个“快”，它们各自承包了互联网中不同类型的业务：

#### 什么时候用 TCP？（要“稳”）
```只要数据不能丢、不能错，哪怕慢一点也行的场景，都用TCP。```

+ 网页浏览（HTTP/HTTPS）：网页源代码少一个字符，页面可能就乱码了。

+ 文件传输（FTP）：下载安装包或压缩文件，如果丢包导致数据损坏，文件就无法打开。

+ 邮件收发（SMTP）：邮件正文和附件必须完整无损。

+ 数据库连接：金融、订单数据的写入，绝对不允许丢失。

#### 什么时候用 UDP？（要“快”）
```只要速度第一，偶尔丢失一点数据不影响大局的场景，都用UDP。```

+ 实时音视频通话（微信/钉钉视频）：画面偶尔卡顿或花屏一下没关系，但如果为了等一个丢包而重传，就会造成严重延迟，对话就断断续续了。

+ 在线游戏：尤其是射击、MOBA类游戏，玩家的位置和动作数据必须极速送达，丢失一两帧坐标数据，比延迟卡顿要好得多。

+ DNS域名解析：在你之前问“DNS劫持”时，DNS查询默认用的就是UDP。因为查询请求非常短小精悍，只需要发一个包、回一个包，用UDP最快，没必要建立复杂的TCP连接。

+ 物联网传感器：传输温度、湿度等高频监控数据，少量丢失不影响整体统计。

# 35. 简要介绍三次握手和四次挥手。
### 三次握手（建立连接）
核心目的：确认双方的发送和接收能力都正常，并同步初始序列号，防止旧连接的延迟数据干扰新连接。

过程如下（假设客户端主动发起）：

+ 第一次握手（SYN）：客户端说：“我要连接你，这是我的初始序号。”（客户端进入 SYN_SENT 状态）

+ 第二次握手（SYN + ACK）：服务器回复：“收到你的序号了，同时这是我的初始序号。”（服务器进入 SYN_RCVD 状态）

+ 第三次握手（ACK）：客户端说：“收到你的序号了，确认无误。”（双方进入 ESTABLISHED 状态，可以开始传数据）

```为什么是三次不是两次？ 因为必须让服务器也确认自己发出的序号被客户端收到了（第三次ACK）。如果没有第三次，服务器不知道客户端是否做好了准备，会白白浪费资源等待。```


### 四次挥手（断开连接）
核心目的：由于TCP是全双工通信（双方可以同时收发），断开时必须分别关闭两个方向的数据传输，确保双方的数据都完整发送完毕。

过程如下（假设客户端主动关闭）：

+ 第一次挥手（FIN）：客户端说：“我的数据发完了，我想关闭发送通道。”（客户端进入 FIN_WAIT_1 状态）

+ 第二次挥手（ACK）：服务器回复：“收到你的关闭请求。”（服务器进入 CLOSE_WAIT 状态，客户端进入 FIN_WAIT_2 状态）

```此时，客户端不再发送数据，但服务器可能还有数据要发给客户端，仍然可以继续发送。```

+ 第三次挥手（FIN）：服务器数据也发完了，说：“我也可以关闭了。”（服务器进入 LAST_ACK 状态）

+ 第四次挥手（ACK）：客户端回复：“收到，确认关闭。”（客户端进入 TIME_WAIT 状态，等待2MSL后彻底关闭）

```为什么是四次不是三次？ 因为“关闭发送通道”和“数据发送完毕”这两个动作在服务器侧是分开的。服务器收到客户端的FIN后，得先回复ACK表示知晓，等自己的数据全部发送完毕，才能再发一次FIN去关闭自己的通道。ACK和FIN不能合并，所以比握手多一次。```

# 36. 什么是粘包？socket 中造成粘包的原因是什么？哪些情况会发生粘包？
```粘包指的是：接收方一次读到的数据里，把多个「本该分开」的数据包粘在了一起，或者把一个完整的数据包拆成了几段读到（拆包）。本质是数据的“边界”丢失了——接收方拿到一串字节流，无法区分里面到底有几条消息、每条消息从哪到哪。```

**核心前提：粘包只发生在 TCP，不会发生在 UDP。**

+ TCP 是**面向字节流**的协议：数据在它眼里就是一长串没有边界的字节流，它只保证顺序和可靠，不关心你发的是“几条消息”。
+ UDP 是**面向数据报**的协议：每个包都是独立、有边界的，收发一一对应，所以天然不粘包。

### 造成粘包的根本原因
根本原因就一句话：**TCP 是流式协议，本身没有消息边界，而应用层又没有自己定义边界。**

底层机制有两个方向：

+ **发送方的 Nagle 算法**：为了提高网络效率，TCP 会把多个小的、间隔很短的数据包**攒在一起**合并成一个大包再发出去，导致接收方一次收到多条消息（发送端粘包）。
+ **接收方的缓冲区**：数据先进入接收缓冲区，应用程序调用 `recv` 的时机和读取的大小不固定。如果读得慢、来得快，多条消息就会堆在缓冲区里被一次性读走；如果一条消息太大超过一次读取量，就会被拆开分几次读（拆包）。

### 哪些情况会发生粘包？
**情况一：发送方粘包（多条粘成一条）**
+ 发送数据时间间隔很短，数据又很小，被 Nagle 算法合并成一个 TCP 包一起发出。
+ 例如连续 `send("hello")`、`send("world")`，接收方一次 `recv` 收到 `"helloworld"`。

**情况二：接收方粘包（读取不及时）**
+ 接收方没有及时 `recv`，数据在缓冲区堆积，下次一次性全部读出，造成多条消息粘连。

**情况三：拆包（一条被拆成多条）**
+ 单条消息大于 TCP 缓冲区剩余空间，或大于 MSS（最大报文段长度），被拆分成多个 TCP 段传输，接收方需要多次 `recv` 才能读完一条完整消息。

### 如何解决粘包？（补充）
解决思路是：**在应用层自己定义消息边界**，常见三种方案：

| 方案 | 做法 | 说明 |
| --- | --- | --- |
| 固定长度 | 每条消息都补齐到固定字节数 | 简单，但浪费空间、不灵活 |
| 特殊分隔符 | 消息之间用如 `\n`、`\r\n` 分隔 | 需保证正文里不出现分隔符（如 Redis、HTTP 头） |
| 消息头 + 长度字段（最常用） | 先发 4 字节表示正文长度，再发正文 | 接收方先读长度，再按长度精确读正文，彻底解决粘包/拆包 |

> 一句话总结：粘包是 TCP 字节流协议“没有边界”导致的现象，发送端 Nagle 合并、接收端读取不及时、单包过大拆分都会引发。UDP 不会粘包。根治方法是应用层自定义边界，工程上最常用“消息头带长度字段”。

# 37. 进程之间如何通信？（IPC）
```进程之间内存相互隔离，不能直接访问对方的变量，必须借助操作系统提供的机制来通信，这些机制统称 IPC（Inter-Process Communication）。```

### 1. 管道（Pipe）
+ **匿名管道**：半双工（单向），只能用于**有亲缘关系**的进程（如父子进程）。数据像水流一样单向流动，写端写、读端读。
+ **命名管道（FIFO）**：有一个文件名，突破了亲缘限制，**任意两个进程**都能通过它通信。
+ 特点：简单，但只能传字节流，容量有限。

````python
from multiprocessing import Process, Pipe

def child(conn):
    conn.send("你好，我是子进程")        # 往管道写
    print("子进程收到:", conn.recv())    # 从管道读
    conn.close()

if __name__ == "__main__":
    parent_conn, child_conn = Pipe()      # 返回管道两端
    p = Process(target=child, args=(child_conn,))
    p.start()
    print("父进程收到:", parent_conn.recv())  # 收子进程发的
    parent_conn.send("收到，我是父进程")        # 回一条
    p.join()
````

### 2. 消息队列（Message Queue）
+ 内核维护一个消息链表，进程按**消息类型**收发，收发双方不必同时在线。
+ 特点：解决了管道只能传无格式字节流的问题，可按类型读取，但单条消息有大小限制。

````python
from multiprocessing import Process, Queue

def producer(q):
    for i in range(3):
        q.put(f"消息{i}")          # 入队

def consumer(q):
    for _ in range(3):
        print("消费:", q.get())    # 出队（队列空时会阻塞等待）

if __name__ == "__main__":
    q = Queue()
    p1 = Process(target=producer, args=(q,))
    p2 = Process(target=consumer, args=(q,))
    p1.start(); p2.start()
    p1.join(); p2.join()
````

### 3. 共享内存（Shared Memory）
+ 多个进程映射同一块物理内存，直接读写，**速度最快**（数据不经过内核拷贝）。
+ 缺点：本身不提供同步机制，需要配合**信号量**来防止读写冲突（竞态）。

````python
from multiprocessing import Process, Value, Array

def worker(num, arr):
    num.value = 3.14              # 直接改共享内存里的值
    for i in range(len(arr)):
        arr[i] *= 2

if __name__ == "__main__":
    num = Value('d', 0.0)             # 'd' = double，共享一个浮点数
    arr = Array('i', [1, 2, 3, 4])   # 'i' = int，共享一个整型数组
    p = Process(target=worker, args=(num, arr))
    p.start(); p.join()
    print(num.value)     # 3.14
    print(arr[:])        # [2, 4, 6, 8]

# Python 3.8+ 更底层的 shared_memory：
from multiprocessing import shared_memory
shm = shared_memory.SharedMemory(create=True, size=10)
shm.buf[0] = 65          # 直接写字节
print(shm.buf[0])        # 65
shm.close(); shm.unlink()
````

### 4. 信号量（Semaphore）
+ 本质是一个计数器，用于**进程/线程同步与互斥**，控制多个进程对共享资源的访问，常与共享内存搭配使用。它更多是“协调”而非“传数据”。

````python
from multiprocessing import Process, Semaphore
import time

def worker(sem, i):
    with sem:                        # 拿到信号量才能进入（最多2个同时进）
        print(f"进程{i} 进入")
        time.sleep(1)
        print(f"进程{i} 离开")

if __name__ == "__main__":
    sem = Semaphore(2)               # 计数器=2，同时只允许2个进程
    ps = [Process(target=worker, args=(sem, i)) for i in range(5)]
    for p in ps: p.start()
    for p in ps: p.join()
````

### 5. 信号（Signal）
+ 一种**异步通知**机制，用于通知进程发生了某个事件。例如 `kill -9`、`Ctrl+C`（SIGINT）。只能传递“发生了什么事”，不能传大量数据。

````python
import signal, time, os

def handler(signum, frame):          # 收到信号时触发
    print(f"收到信号 {signum}，准备退出")
    exit(0)

signal.signal(signal.SIGINT, handler)    # 注册：Ctrl+C -> handler
# signal.signal(signal.SIGTERM, handler) # kill 默认信号

print(f"我的PID={os.getpid()}，按 Ctrl+C 试试")
while True:
    time.sleep(1)
````

### 6. 套接字（Socket）
+ 最通用的方式，**既能本机进程通信，也能跨网络、跨主机通信**。TCP/UDP 都基于它，分布式系统中进程通信几乎都靠它。

````python
# 服务端
import socket
s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
s.bind(("127.0.0.1", 8888))
s.listen()
print("等待连接...")
conn, addr = s.accept()
print("收到:", conn.recv(1024).decode())
conn.send("服务端已收到".encode())
conn.close()

# 客户端
import socket
s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
s.connect(("127.0.0.1", 8888))
s.send("你好，服务端".encode())
print("回复:", s.recv(1024).decode())
s.close()
````

### 速查对比

| 方式 | 速度 | 数据量 | 适用场景 |
| --- | --- | --- | --- |
| 管道 | 一般 | 小 | 父子进程/本机简单通信 |
| 消息队列 | 一般 | 中 | 需要按类型、异步收发 |
| 共享内存 | **最快** | 大 | 本机大量数据高频交换（需配信号量） |
| 信号量 | — | 无（只同步） | 控制资源访问、进程同步 |
| 信号 | 快 | 极小 | 异步事件通知 |
| Socket | 较慢 | 大 | 跨主机 / 分布式 / 网络通信 |

> 一句话总结：本机小数据用**管道/消息队列**，本机大数据高频用**共享内存 + 信号量**，只做通知用**信号**，跨机器用**Socket**。

**Python 视角：** `multiprocessing` 模块封装了这些机制——`Pipe()` 对应管道，`Queue()` 底层是管道+锁，`Value`/`Array`/`shared_memory` 对应共享内存，`Lock`/`Semaphore` 对应信号量。

# 38. IO 多路复用的作用？
```用一个（或少量）线程，同时监听管理成千上万个 IO 连接，哪个连接的数据准备好了就处理哪个，避免为每个连接都开一个线程/进程。```

### 为什么需要它？（解决的痛点）
没有它的两种笨办法：

+ **阻塞 IO + 单线程**：一次只能处理一个连接，`recv` 时会卡住，别的连接只能干等，无法并发。
+ **阻塞 IO + 多线程**：给每个连接开一个线程。连接数一多（比如 1 万个），就要 1 万个线程，**内存爆炸、CPU 频繁切换上下文**，系统扛不住（C10K 问题）。

IO 多路复用的思路：**一个线程盯着一堆连接，由操作系统内核帮你监控哪些连接“就绪”了**，就绪了才去读写，没就绪的不占用线程资源。

### 核心作用总结

| 作用 | 说明 |
| --- | --- |
| 单线程管理大量连接 | 一个线程可监听上万个 socket，省下大量线程/进程开销 |
| 避免无效阻塞/轮询 | 内核通知“哪个就绪”，程序不用挨个盲目试探 |
| 高并发、高吞吐 | 是 Redis、Nginx、Node.js 等高性能服务的底层基石 |

### 三种实现方式对比（重点）

| 机制 | 原理 | 缺点 / 优点 |
| --- | --- | --- |
| **select** | 把所有 fd 放进一个集合，轮询检查谁就绪 | 有数量上限（默认 1024）；每次都要把整个集合从用户态拷到内核态，O(n) 遍历，慢 |
| **poll** | 和 select 类似，但用链表，无数量上限 | 解决了数量限制，但仍是 O(n) 全量轮询，量大依然慢 |
| **epoll**（Linux 主流） | 内核用红黑树管理 fd + 就绪链表，事件驱动回调 | **只返回就绪的 fd，O(1) 级别**；无数量限制；高并发首选。Redis/Nginx 都用它 |

> 补充：epoll 有两种触发模式——**水平触发（LT）**：只要还有数据没读完就一直通知；**边缘触发（ET）**：只在状态变化时通知一次，效率更高但编程更难，必须一次读干净。

### 一个生活类比
> 一个前台（线程）同时看管 100 个包间（连接）。不用给每个包间配一个服务员（多线程），前台只要盯着“叫号灯”（内核事件通知），哪个包间亮灯了（数据就绪）就去服务哪个，其余时间不用来回瞎跑。

**Python 视角：** 标准库 `select` 模块封装了 `select` / `poll` / `epoll`；`selectors` 模块是更高层的统一封装（自动选最优机制）；`asyncio` 事件循环底层就基于 IO 多路复用。

# 39. 元类（Metaclass）详解
```元类是“创建类的类”。普通的类是用来创建对象（实例）的模板，而元类是用来创建“类”本身的模板。```

````
元类  ──创建──▶  类  ──创建──▶  实例
type          Dog          dog = Dog()
````

## 39.1 先理解一个事实：类也是对象
在 Python 里，**一切皆对象，类本身也是一个对象**。既然是对象，它就一定是被某个东西创建出来的——那个东西就是元类。
````python
class Dog:
    pass

dog = Dog()          # 用 Dog 创建实例 dog
print(type(dog))     # <class '__main__.Dog'>  —— dog 的类型是 Dog
print(type(Dog))     # <class 'type'>           —— Dog 的类型是 type！
````
`Dog` 这个类的“类型”是 `type`。也就是说 **`type` 就是 Python 默认的元类**，所有类默认都由 `type` 创建。

## 39.2 type 的双重身份
````python
# 用法一：查看一个对象的类型
print(type(123))     # <class 'int'>

# 用法二：动态创建一个类！（type(类名, 父类元组, 属性字典)）
Dog = type("Dog", (), {"sound": "汪汪"})
d = Dog()
print(d.sound)       # 汪汪
````
下面两种写法**完全等价**：
````python
# 常规写法
class Dog:
    sound = "汪汪"

# 用 type 动态创建（本质就是这个）
Dog = type("Dog", (), {"sound": "汪汪"})
````
> 所以你平时写的 `class` 关键字，本质上就是 Python 在背后调用 `type(...)` 帮你创建类。

## 39.3 自定义元类
自定义元类就是**继承 `type`**，然后重写它的 `__new__` 或 `__call__`，从而**干预类的创建过程**。
````python
class Meta(type):
    def __new__(mcs, name, bases, attrs):
        # mcs: 元类自己; name: 类名; bases: 父类; attrs: 类的属性字典
        print(f"正在创建类：{name}")
        attrs["author"] = "张三"        # 给这个类偷偷加一个属性
        return super().__new__(mcs, name, bases, attrs)

class MyClass(metaclass=Meta):        # 指定用 Meta 作元类
    pass

# 输出：正在创建类：MyClass （定义时就触发，不用实例化）
print(MyClass.author)                 # 张三 （被元类塞进来的）
````

## 39.4 `__new__` vs `__init__` vs `__call__`（元类里的时机）
| 方法 | 触发时机 | 作用 |
| --- | --- | --- |
| 元类 `__new__` | **创建类时** | 修改/生成类本身（改属性、改方法） |
| 元类 `__init__` | **类创建后初始化** | 对已创建的类做初始化设置 |
| 元类 `__call__` | **实例化对象时**（`MyClass()`） | 控制实例的创建过程（单例就靠它） |

单例元类正是重写了 `__call__`：
````python
class SingletonMeta(type):
    _instances = {}
    def __call__(cls, *args, **kwargs):     # 每次 Logger() 都会走这里
        if cls not in cls._instances:
            cls._instances[cls] = super().__call__(*args, **kwargs)
        return cls._instances[cls]

class Logger(metaclass=SingletonMeta):
    pass
````

## 39.5 元类到底有什么用？（真实场景）
平时开发**几乎用不到**元类，但框架底层大量使用：

| 场景 | 说明 |
| --- | --- |
| ORM 框架 | Django/SQLAlchemy 的 `class User(Model)`，元类扫描你定义的字段，自动映射成数据库表结构 |
| 接口/抽象强制 | `ABCMeta` 就是元类，强制子类必须实现某些方法 |
| 单例模式 | 如上，用 `__call__` 拦截实例化 |
| 自动注册 | 定义一个类就自动把它注册到某个中心（如插件系统、路由表） |
| API 校验 | 类定义时就检查是否符合规范，不符合直接报错，把错误提前到“定义期” |

**自动注册示例：**
````python
registry = {}

class AutoRegister(type):
    def __new__(mcs, name, bases, attrs):
        cls = super().__new__(mcs, name, bases, attrs)
        if name != "Plugin":            # 排除基类自己
            registry[name] = cls        # 定义即注册
        return cls

class Plugin(metaclass=AutoRegister): pass
class EmailPlugin(Plugin): pass
class SMSPlugin(Plugin): pass

print(registry)  # {'EmailPlugin': <class...>, 'SMSPlugin': <class...>}
````

## 39.6 一句忠告
> Python 之父之一 Tim Peters 的名言：
> **“元类是 99% 的用户永远不需要担心的深度魔法。如果你在纠结要不要用元类，那你就不需要用（真正需要的人非常清楚自己为什么要用）。”**

日常能用**类装饰器**、**继承**解决的，就别上元类。元类是框架作者的工具，理解它主要是为了**看懂框架源码**和**应付面试**。

## 39.7 总结
+ **是什么**：创建类的类，默认元类是 `type`。
+ **本质**：`class` 关键字底层就是调用 `type(name, bases, attrs)`。
+ **怎么用**：继承 `type`，重写 `__new__`（改类）/ `__call__`（改实例化）。
+ **用在哪**：ORM、ABC、单例、自动注册等框架级需求。
+ **原则**：能不用就不用，但要看得懂。

# 40. Python 并发编程完整教程：进程、线程、协程

## 40.1 并发 vs 并行
````
并发 (Concurrency)          并行 (Parallelism)
   多任务交替执行              多任务同时执行
   单核也能并发                必须多核
   ↓                          ↓
   协程、线程                  多进程、多核线程
````
**举例：**
+ 你边吃饭边看手机 → **并发**（交替切换注意力）
+ 你和另一个人同时各做一件事 → **并行**

## 40.2 GIL（全局解释器锁）
+ **什么是 GIL**：CPython 解释器中一把全局互斥锁，保证同一时刻只有一个线程执行 Python 字节码。
+ **为什么有 GIL**：CPython 用引用计数管理内存，多线程同时修改引用计数会出问题。GIL 是最简单的解决方案。

**GIL 的影响：**
````python
# ❌ CPU 密集任务：多线程没有加速，反而更慢
def count(n):
    while n > 0:
        n -= 1

# 单线程
count(10**8)              # 约 3 秒

# 多线程（受 GIL 限制，不会加速）
t1 = Thread(target=count, args=(10**8//2,))
t2 = Thread(target=count, args=(10**8//2,))
# 总耗时 ≈ 3 秒（甚至更慢）
````
**GIL 何时释放：**
+ IO 操作（read/write/socket）会主动释放 GIL
+ 每 100 条字节码（Python 3.2+）会强制释放一次
+ 调用 C 扩展时可能释放

> 这就是为什么多线程对 **IO 密集**任务有效。

## 40.3 进程（multiprocessing）
### 40.3.1 创建进程
````python
from multiprocessing import Process
import os

def worker(msg):
    print(f"子进程 pid={os.getpid()}, 父pid={os.getppid()}, msg={msg}")

if __name__ == "__main__":
    p = Process(target=worker, args=("hello",))
    p.start()       # 启动子进程
    p.join()        # 等待子进程结束
    print(f"主进程 pid={os.getpid()}")
````
> ⚠️ Windows 必须 `if __name__ == "__main__":` 保护入口，否则会无限递归创建子进程。

### 40.3.2 自定义 Process 类
````python
class MyProcess(Process):
    def __init__(self, value):
        super().__init__()
        self.value = value

    def run(self):           # 重写 run 方法
        print(f"运行任务: {self.value}")

p = MyProcess(42)
p.start()    # 自动调用 run()
p.join()
````

### 40.3.3 进程池（推荐方式）
````python
from multiprocessing import Pool

def task(x):
    return x ** 2

if __name__ == "__main__":
    with Pool(4) as pool:
        # 方式一：map（阻塞，有序）
        print(pool.map(task, range(10)))
        # [0, 1, 4, 9, 16, 25, 36, 49, 64, 81]

        # 方式二：imap（惰性，有序）
        for r in pool.imap(task, range(10)):
            print(r, end=" ")

        # 方式三：apply_async（异步）
        results = [pool.apply_async(task, (i,)) for i in range(10)]
        print([r.get() for r in results])

        # 方式四：starmap（多参数）
        def add(a, b): return a + b
        print(pool.starmap(add, [(1,2),(3,4),(5,6)]))
````

### 40.3.4 进程间通信
**Queue（队列）：**
````python
from multiprocessing import Process, Queue

def producer(q):
    for i in range(5):
        q.put(f"产品{i}")
    q.put(None)    # 结束信号

def consumer(q):
    while True:
        item = q.get()
        if item is None: break
        print(f"消费: {item}")

if __name__ == "__main__":
    q = Queue()
    p = Process(target=producer, args=(q,))
    c = Process(target=consumer, args=(q,))
    p.start(); c.start()
    p.join(); c.join()
````
**Pipe（管道，双向）：**
````python
from multiprocessing import Process, Pipe

def child(conn):
    conn.send("爸爸你好")
    print("收到:", conn.recv())
    conn.close()

if __name__ == "__main__":
    parent_conn, child_conn = Pipe()
    p = Process(target=child, args=(child_conn,))
    p.start()
    print("收到:", parent_conn.recv())
    parent_conn.send("儿子你好")
    p.join()
````
**共享内存（Value / Array / Lock）：**
````python
from multiprocessing import Process, Value, Array, Lock

def increment(counter, lock):
    for _ in range(1000):
        with lock:
            counter.value += 1

if __name__ == "__main__":
    counter = Value('i', 0)   # 'i' = 整型
    lock = Lock()
    ps = [Process(target=increment, args=(counter, lock)) for _ in range(5)]
    [p.start() for p in ps]
    [p.join() for p in ps]
    print(counter.value)      # 5000
````
**Manager（更灵活，支持 dict/list）：**
````python
from multiprocessing import Manager

with Manager() as m:
    d = m.dict()
    l = m.list(range(5))
    # 在多进程间共享
````

## 40.4 线程（threading）
### 40.4.1 创建线程
````python
import threading
import time

def worker(name):
    for i in range(3):
        time.sleep(0.5)
        print(f"[{name}] 第{i+1}次, 线程={threading.current_thread().name}")

t1 = threading.Thread(target=worker, args=("A",), name="T1")
t2 = threading.Thread(target=worker, kwargs={"name": "B"}, name="T2")
t1.start(); t2.start()
t1.join(); t2.join()
````

### 40.4.2 线程的生命周期方法
````python
t = threading.Thread(target=worker, args=("A",))
t.start()        # 启动
t.join(2)        # 最多等 2 秒
t.is_alive()     # 是否在运行
t.name           # 线程名
t.daemon         # 是否是守护线程
````

### 40.4.3 守护线程
主线程结束时，守护线程会立即终止（不会等待它完成）。
````python
def heartbeat():
    while True:
        print("心跳...")
        time.sleep(1)

t = threading.Thread(target=heartbeat)
t.daemon = True    # 必须在 start() 前设置
t.start()
time.sleep(3)
print("主线程结束，守护线程随之终止")
````

### 40.4.4 同步原语
**Lock（互斥锁）：**
````python
lock = threading.Lock()

def critical_section():
    lock.acquire()
    try:
        # 临界区
        pass
    finally:
        lock.release()

# 推荐用 with 语法（自动释放）
with lock:
    pass
````
**RLock（可重入锁）：** 同一个线程可多次 acquire。
````python
rlock = threading.RLock()
````
**Semaphore（信号量，限流）：**
````python
sem = threading.Semaphore(3)   # 最多 3 个线程同时访问

def worker():
    with sem:
        # 最多 3 个线程同时执行
        pass
````
**Event（事件通知）：**
````python
event = threading.Event()

def waiter():
    print("等待中...")
    event.wait()       # 阻塞直到 set()
    print("收到信号!")

# 主线程
event.set()           # 触发
````
**Condition（条件变量）：**
````python
cond = threading.Condition()

def producer():
    with cond:
        cond.notify_all()   # 通知所有等待者

def consumer():
    with cond:
        cond.wait()         # 等待通知
````

### 40.4.5 线程局部数据（ThreadLocal）
每个线程独立一份，无需加锁。
````python
local_data = threading.local()

def worker():
    local_data.user = threading.current_thread().name
    print(local_data.user)

for i in range(3):
    threading.Thread(target=worker).start()
````

## 40.5 协程（asyncio）
### 40.5.1 核心概念
+ **事件循环 (Event Loop)**：调度协程的核心
+ **协程函数**：`async def` 定义的函数
+ **任务 (Task)**：被事件循环调度的协程
+ **Future**：异步结果的容器
+ **await**：让出控制权，等待结果

### 40.5.2 基础语法
````python
import asyncio

async def hello():
    print("Hello")
    await asyncio.sleep(1)    # 异步等待（让出 CPU）
    print("World")
    return "done"

# 运行协程
result = asyncio.run(hello())
print(result)
````

### 40.5.3 并发执行
````python
async def task(name, sec):
    print(f"[{name}] 开始")
    await asyncio.sleep(sec)
    print(f"[{name}] 完成")
    return name

async def main():
    # gather：并发执行所有协程，等所有完成
    results = await asyncio.gather(
        task("A", 2),
        task("B", 1),
        task("C", 3),
    )
    print(results)   # 总耗时约 3 秒（最长任务）

asyncio.run(main())
````

### 40.5.4 Task vs gather vs wait
````python
async def main():
    # 方式一：create_task（手动管理）
    t1 = asyncio.create_task(task("A", 1))
    t2 = asyncio.create_task(task("B", 2))
    await t1
    await t2

    # 方式二：gather（推荐，保留顺序）
    await asyncio.gather(t1, t2)

    # 方式三：wait（更细粒度控制）
    done, pending = await asyncio.wait(
        [t1, t2],
        return_when=asyncio.FIRST_COMPLETED,  # 第一个完成就返回
        timeout=5,
    )
````

### 40.5.5 超时与取消
````python
async def slow_task():
    await asyncio.sleep(10)
    return "done"

async def main():
    # 超时
    try:
        result = await asyncio.wait_for(slow_task(), timeout=2)
    except asyncio.TimeoutError:
        print("任务超时!")

    # 取消
    t = asyncio.create_task(slow_task())
    t.cancel()
    try:
        await t
    except asyncio.CancelledError:
        print("任务被取消")
````

### 40.5.6 异步上下文管理器 & 迭代器
````python
class AsyncResource:
    async def __aenter__(self):
        print("获取资源")
        return self

    async def __aexit__(self, *args):
        print("释放资源")

async def main():
    async with AsyncResource() as r:
        print("使用资源")

# 异步迭代器
class AsyncCounter:
    def __init__(self, stop):
        self.current = 0
        self.stop = stop

    def __aiter__(self):
        return self

    async def __anext__(self):
        if self.current >= self.stop:
            raise StopAsyncIteration
        await asyncio.sleep(0.5)
        self.current += 1
        return self.current

async def main():
    async for num in AsyncCounter(5):
        print(num)
````

### 40.5.7 异步 IO 实战
````python
import asyncio
import aiohttp

async def fetch(session, url):
    async with session.get(url, timeout=5) as resp:
        return resp.status, len(await resp.text())

async def crawl(urls):
    sem = asyncio.Semaphore(10)    # 并发限制 10

    async def limited_fetch(url):
        async with sem:
            return await fetch(session, url)

    async with aiohttp.ClientSession() as session:
        return await asyncio.gather(*[limited_fetch(u) for u in urls])

urls = ["https://example.com"] * 50
results = asyncio.run(crawl(urls))
print(results)
````

### 40.5.8 在同步代码中调用协程
````python
import asyncio

async def async_func():
    await asyncio.sleep(1)
    return "result"

def sync_caller():
    # 方式一：新事件循环
    result = asyncio.run(async_func())

    # 方式二：在已有循环中（慎用，可能死锁）
    # loop = asyncio.get_event_loop()
    # result = asyncio.run_coroutine_threadsafe(async_func(), loop).result()
````

## 40.6 线程池与进程池（concurrent.futures）
统一 API，让多线程/多进程用法一致。
````python
from concurrent.futures import ThreadPoolExecutor, ProcessPoolExecutor

def task(x):
    return x ** 2

# 线程池（IO 密集）
with ThreadPoolExecutor(max_workers=10) as pool:
    futures = [pool.submit(task, i) for i in range(10)]
    results = [f.result() for f in futures]

# 进程池（CPU 密集）
with ProcessPoolExecutor(max_workers=4) as pool:
    results = list(pool.map(task, range(10)))

# as_completed（谁完成谁先返回）
from concurrent.futures import as_completed
with ThreadPoolExecutor(10) as pool:
    futures = {pool.submit(task, i): i for i in range(10)}
    for f in as_completed(futures):
        print(f.result())
````

## 40.7 性能对比实战
### 40.7.1 CPU 密集：计算斐波那契
````python
import time
from multiprocessing import Pool
from concurrent.futures import ThreadPoolExecutor

def fib(n):
    return n if n < 2 else fib(n-1) + fib(n-2)

NUMS = [35] * 4

# 串行
t0 = time.time()
[fib(n) for n in NUMS]
print(f"串行: {time.time()-t0:.2f}s")

# 多线程（GIL 限制，无加速）
t0 = time.time()
with ThreadPoolExecutor(4) as p:
    list(p.map(fib, NUMS))
print(f"多线程: {time.time()-t0:.2f}s")

# 多进程（真并行）
t0 = time.time()
with Pool(4) as p:
    p.map(fib, NUMS)
print(f"多进程: {time.time()-t0:.2f}s")
````
**典型结果：**
````
串行:    4.5s
多线程:  5.0s  （比串行还慢）
多进程:  1.5s  （约 3 倍加速）
````

### 40.7.2 IO 密集：模拟请求
````python
import time, asyncio
from concurrent.futures import ThreadPoolExecutor

def io_task():
    time.sleep(1)
    return "done"

# 多线程
t0 = time.time()
with ThreadPoolExecutor(10) as p:
    list(p.map(lambda _: io_task(), range(10)))
print(f"多线程: {time.time()-t0:.2f}s")    # ~1s

# 协程
async def async_io():
    await asyncio.sleep(1)

async def main():
    t0 = time.time()
    await asyncio.gather(*[async_io() for _ in range(10)])
    print(f"协程: {time.time()-t0:.2f}s")  # ~1s

asyncio.run(main())
````

## 40.8 常见陷阱
### 40.8.1 线程不安全
````python
# ❌ 错误：未加锁
counter = 0
def add():
    global counter
    for _ in range(100000):
        counter += 1   # 非原子操作：读-改-写

# ✅ 正确：加锁
from threading import Lock
lock = Lock()
def add_safe():
    global counter
    for _ in range(100000):
        with lock:
            counter += 1
````

### 40.8.2 死锁
````python
# ❌ 锁顺序不一致导致死锁
def f1():
    with lockA:
        with lockB:    # 等待 B
            pass

def f2():
    with lockB:
        with lockA:    # 等待 A —— 死锁
            pass
````
> 避免：所有地方按相同顺序获取锁。

### 40.8.3 协程中的阻塞调用
````python
# ❌ 错误：time.sleep 阻塞整个事件循环
async def bad():
    time.sleep(1)         # 阻塞！其他协程无法运行

# ✅ 正确：用 asyncio.sleep
async def good():
    await asyncio.sleep(1)    # 让出 CPU
````
在协程中调用同步阻塞库（如 requests）会拖累整个事件循环，应使用异步库（aiohttp）或扔进线程池：
````python
async def main():
    loop = asyncio.get_event_loop()
    result = await loop.run_in_executor(None, blocking_func)
````

### 40.8.4 进程间对象无法共享
````python
# ❌ 进程间不能直接共享 Python 对象
def worker(d):
    d["x"] = 1     # 子进程的副本，主进程看不到

d = {}
p = Process(target=worker, args=(d,))
# 用 Manager 替代普通 dict
````

### 40.8.5 Windows 下的 `if __name__ == "__main__":`
Windows 没有 fork，子进程会重新导入主模块，不加保护会无限递归创建进程。

## 40.9 选型决策指南
````
你的任务是什么？
│
├─【CPU 密集】计算、加密、图像处理、机器学习推理
│   └─ multiprocessing / ProcessPoolExecutor
│       - 完全绕过 GIL
│       - 进程数 = CPU 核心数（os.cpu_count()）
│
├─【IO 密集 - 高并发】爬虫、API、WebSocket、数据库
│   └─ asyncio + 异步库（aiohttp、asyncpg）
│       - 单线程，几万并发
│       - 切换成本极低
│
├─【IO 密集 - 低并发】或必须用同步库
│   └─ threading / ThreadPoolExecutor
│       - 5~50 个线程足够
│       - IO 期间释放 GIL
│
└─【混合任务】
    └─ 多进程 + 协程组合
        - 进程层：多核并行 CPU
        - 协程层：每进程内处理海量 IO
````

## 40.10 完整速查表
| 场景 | 推荐方案 | 模块 |
| --- | --- | --- |
| CPU 密集 + 多核 | 进程池 | `multiprocessing.Pool` |
| 网络爬虫 | 协程 + aiohttp | `asyncio` |
| 文件/数据库 IO | 线程池 | `concurrent.futures.ThreadPoolExecutor` |
| 实时音视频 | 进程 | `multiprocessing` |
| GUI 应用后台任务 | 线程（守护） | `threading` |
| 海量定时任务 | 协程 + 任务队列 | `asyncio + asyncio.Queue` |
| 子进程管理 | 子进程 | `subprocess` |

**关键记忆：**
+ GIL 只影响 **CPU 密集多线程**，不影响 IO。
+ 进程 = 真并行，但通信开销大、内存大。
+ 线程 = 并发，共享内存需加锁。
+ 协程 = 单线程并发，性能最高但需全异步栈。
+ `asyncio.run()` 是入口，`await` 是让出点。
+ `concurrent.futures` 统一了池的 API，最易上手。
+ 不要在协程里写同步阻塞调用，会拖垮整个循环。

# 41. 服务端渲染（SSR，Server-Side Rendering）
```网页的 HTML 内容在服务端就生成好，再直接返回给浏览器，而不是只返回一个空壳 HTML + JS，让浏览器再去执行 JS 渲染。```

## 41.1 两种渲染方式对比
| | 客户端渲染 CSR | 服务端渲染 SSR |
| --- | --- | --- |
| **首次请求的响应** | 返回一个几乎空的 HTML 和一堆 JS | 返回已经填充好内容的完整 HTML |
| **首屏内容出现速度** | 慢（要等 JS 下载、执行、请求数据） | 快（HTML 拿来就能显示） |
| **SEO 友好性** | 差（搜索引擎抓取不到 JS 渲染后的内容） | 好（搜索引擎直接看到完整内容） |
| **首屏后交互** | 流畅（页面不刷新，局部更新） | 可能需要 hydration 接管后交互 |
| **服务器压力** | 小（渲染在浏览器做） | 大（每次请求服务端都要渲染） |
| **典型代表** | Vue SPA、React SPA（create-react-app） | Next.js、Nuxt.js |

## 41.2 直观例子
**CSR（客户端渲染）** 浏览器拿到的 HTML 大概长这样：
````html
<!DOCTYPE html>
<html>
  <head>...</head>
  <body>
    <div id="root"></div>   <!-- 空的！ -->
    <script src="app.js"></script>  <!-- JS 来填内容 -->
  </body>
</html>
````
然后浏览器下载 `app.js` → 执行 JS → 请求 API 数据 → 才把页面填出来。

**SSR（服务端渲染）** 浏览器直接拿到：
````html
<!DOCTYPE html>
<html>
  <head><title>文章标题</title></head>
  <body>
    <h1>文章标题</h1>
    <p>这是一篇已经渲染好的文章内容...</p>
    <script src="app.js"></script>  <!-- 后续接管交互 -->
  </body>
</html>
````
首屏内容立刻可见，JS 后续再“复活”交互（这个过程叫 **hydration / 注水**）。

## 41.3 为什么需要 SSR？
**1. SEO（搜索引擎优化）**
Google 虽然能执行 JS，但很多爬虫、社交分享抓取（微信、微博、Twitter）**等不及 JS 执行**，只能看到空页面。SSR 能直接给它们完整 HTML。
> 典型场景：博客、新闻、电商商品详情页、官网。

**2. 首屏体验（FCP/LCP 更快）**
用户打开页面时，**立刻看到内容**，而不是转圈圈等 JS 加载。这对弱网、低端设备尤其重要。

## 41.4 SSR 的工作流程
````
用户请求 /article/123
        ↓
  服务端收到请求
        ↓
  服务端调用 API / 查数据库 取数据
        ↓
  服务端把数据填充进模板/组件，生成完整 HTML
        ↓
  返回 HTML 给浏览器
        ↓
  浏览器展示首屏内容
        ↓
  加载 JS → hydration → 页面可交互
````

## 41.5 现代 SSR 框架
| 框架 | 技术栈 | 说明 |
| --- | --- | --- |
| **Next.js** | React | 最主流，支持 SSR、SSG、ISR |
| **Nuxt.js** | Vue | Vue 生态的 SSR 方案 |
| **SvelteKit** | Svelte | 新兴方案 |
| **Astro** | 多框架 | 默认 0 JS，可做部分 hydration |

## 41.6 SSR 的代价 / 缺点
| 问题 | 说明 |
| --- | --- |
| 服务器压力大 | 每个请求都要渲染，QPS 高时 CPU 内存吃紧 |
| 开发复杂 | 同一份代码要在服务端和浏览器都能跑，注意 window/document 只在浏览器有 |
| hydration 不匹配 | 服务端渲染的 HTML 和客户端 JS 首次渲染不一致会报错 |
| 缓存策略复杂 | 动态内容缓存比纯静态页面麻烦 |

## 41.7 什么时候用 SSR？
| 场景 | 推荐 |
| --- | --- |
| 内容型网站（博客、新闻、文档） | ✅ SSR/SSG |
| 电商商品详情页、需要 SEO | ✅ SSR |
| 后台管理系统（登录后才用） | ❌ CSR 即可 |
| 强交互 Web App（类似 Figma、Notion） | ❌ CSR 更合适 |
| 流量极大、内容变化不频繁 | ✅ SSG（静态生成）或 ISR |

## 41.8 相关概念区分
| 概念 | 说明 |
| --- | --- |
| **SSR** | 每次请求服务端实时渲染 |
| **SSG**（静态站点生成） | 构建时预渲染成静态 HTML，请求直接返回静态文件 |
| **ISR**（增量静态再生） | 先返回静态缓存，后台异步更新，Next.js 特有 |
| **CSR** | 纯浏览器端渲染 |
| **Hydration** | 服务端返回 HTML 后，前端 JS 接管使其可交互 |

> 一句话总结：**SSR = 在服务端把页面 HTML 提前拼好再发给浏览器，换来 SEO 友好和首屏快速，但服务器要承担渲染成本。**

# 42. `threading.local()` 与 `contextvars` 的区别
```
- threading.local()：数据绑定到线程，线程之间隔离。
- contextvars（Python 3.7+）：数据绑定到执行上下文，线程内不同协程/任务之间也隔离。
```

## 42.1 为什么需要 `contextvars`？
`threading.local()` 在**多线程**下工作得很好，但在**协程（asyncio）**下会出问题——因为多个协程运行在同一个线程内，它们会共享同一个 `threading.local()`：

```python
import asyncio
import threading

local = threading.local()

async def task(name):
    local.user = name
    await asyncio.sleep(0.1)  # 让出 CPU，其他协程可能修改 local.user
    print(f"我是 {name}，但 local.user = {local.user}")  # 可能不是自己了！

async def main():
    await asyncio.gather(task("A"), task("B"))

asyncio.run(main())
# 输出可能是：
# 我是 A，但 local.user = B
# 我是 B，但 local.user = B
```

因为 A、B 两个协程在同一个线程里，它们共享同一个 `threading.local()`，后赋值会覆盖前值。

`contextvars` 就是来解决这个问题的：

```python
import asyncio
import contextvars

ctx = contextvars.ContextVar("user")

async def task(name):
    ctx.set(name)
    await asyncio.sleep(0.1)
    print(f"我是 {name}，ctx = {ctx.get()}")  # 正确拿到自己的值

async def main():
    await asyncio.gather(task("A"), task("B"))

asyncio.run(main())
# 输出：
# 我是 B，ctx = B
# 我是 A，ctx = A
```

## 42.2 详细对比
| 对比维度 | `threading.local()` | `contextvars.ContextVar` |
| --- | --- | --- |
| 数据绑定对象 | 线程（Thread） | 执行上下文（Context） |
| 同一进程内 | 多线程间隔离 | 多上下文间隔离 |
| 多线程场景 | ✅ 完全支持 | ✅ 支持（上下文跨线程拷贝） |
| 协程/异步场景 | ❌ 同线程内多个协程会相互覆盖 | ✅ 每个协程/任务独立 |
| 是否需要 set 默认值 | 不需要，直接赋值 | 需要通过 `set()` 设置，取值用 `get()` |
| API 风格 | 像普通对象属性 | 显式的 ContextVar 对象 |
| 标准库引入 | 一直有 | Python 3.7+ |

## 42.3 `contextvars` 的关键 API
```python
import contextvars

user_var = contextvars.ContextVar("user", default="guest")

# 设置值
token = user_var.set("Alice")

# 获取值
print(user_var.get())   # Alice

# 恢复之前的值（重要！避免污染）
user_var.reset(token)
```

> `set()` 会返回一个 token，`reset(token)` 用于恢复到之前的状态。这个机制在异步代码里很重要。

## 42.4 实际应用场景
| 场景 | 推荐方案 |
| --- | --- |
| Web 框架中存储当前请求的用户信息（同步框架如 Flask） | `threading.local()` |
| Web 框架中存储当前请求的用户信息（异步框架如 FastAPI、Sanic、Tornado） | `contextvars` |
| 异步任务链中传递 trace_id、request_id | `contextvars` |
| 线程池里每个线程独立持有数据库连接 | `threading.local()` |
| asyncio 中跨协程传递上下文（如日志、认证信息） | `contextvars` |

## 42.5 现代 Web 框架里的例子
**Flask（同步 WSGI）** 内部用 `threading.local()` 存 `request`、`current_user`：
```python
from flask import request

@app.route("/")
def index():
    print(request.url)   # 当前线程的请求对象
```

**FastAPI（异步 ASGI）** 内部用 `contextvars` 存 `request` 状态，所以你在 async 视图函数里能安全访问当前请求：
```python
from fastapi import Request

@app.get("/")
async def index(request: Request):
    print(request.url)
```

> 一句话总结：**多线程用 `threading.local()`，异步协程用 `contextvars`。`contextvars` 是 Python 为 asyncio 时代补上的“线程局部变量”的进化版。**

# 43. Python 常用魔法函数（Dunder Methods）一览
```
魔法函数就是双下划线开头结尾的方法，如 __init__、__str__，让 Python 知道在特定场景下怎么调用你的对象。
```

## 43.1 对象生命周期相关
| 魔法函数 | 触发时机 | 示例 |
| --- | --- | --- |
| `__new__(cls, ...)` | 创建实例之前调用，真正的“构造” | 单例、不可变子类 |
| `__init__(self, ...)` | 创建实例后初始化 | 最常见，对象初始化 |
| `__del__(self)` | 实例被销毁前调用 | 清理资源，不建议依赖 |
| `__repr__(self)` | 给开发者的字符串表示，`repr(obj)` | 最好 eval(repr(obj)) 能还原 |
| `__str__(self)` | 给用户的字符串表示，`str(obj)`、`print(obj)` | 比 `__repr__` 更友好 |
| `__bytes__(self)` | `bytes(obj)` 时调用 | 二进制表示 |
| `__format__(self, fmt)` | `format(obj, fmt)`、`f"{obj:fmt}"` | 自定义格式化 |
| `__bool__(self)` | `bool(obj)` 时调用 | 决定对象真假 |
| `__hash__(self)` | `hash(obj)`、作为 dict/set 键 | 必须和 `__eq__` 配合 |
| `__dir__(self)` | `dir(obj)` 时调用 | 自定义可查看属性 |

## 43.2 比较操作相关
| 魔法函数 | 触发时机 |
| --- | --- |
| `__eq__(self, other)` | `a == b` |
| `__ne__(self, other)` | `a != b` |
| `__lt__(self, other)` | `a < b` |
| `__le__(self, other)` | `a <= b` |
| `__gt__(self, other)` | `a > b` |
| `__ge__(self, other)` | `a >= b` |

> 用 `@functools.total_ordering` 可以只写 `__eq__` + 一个，自动生成其余比较。

## 43.3 算术运算相关
| 魔法函数 | 触发时机 | 反向运算 |
| --- | --- | --- |
| `__add__(self, other)` | `a + b` | `__radd__` |
| `__sub__(self, other)` | `a - b` | `__rsub__` |
| `__mul__(self, other)` | `a * b` | `__rmul__` |
| `__truediv__(self, other)` | `a / b` | `__rtruediv__` |
| `__floordiv__(self, other)` | `a // b` | `__rfloordiv__` |
| `__mod__(self, other)` | `a % b` | `__rmod__` |
| `__pow__(self, other)` | `a ** b` | `__rpow__` |
| `__matmul__(self, other)` | `a @ b`（矩阵乘） | `__rmatmul__` |
| `__neg__(self)` | `-a` | — |
| `__pos__(self)` | `+a` | — |
| `__abs__(self)` | `abs(a)` | — |
| `__invert__(self)` | `~a` | — |

> 还有原地运算：`__iadd__`、`__isub__` 等，对应 `+=`、`-=`。

## 43.4 位运算相关
| 魔法函数 | 触发时机 |
| --- | --- |
| `__and__(self, other)` | `a & b` |
| `__or__(self, other)` | `a \| b` |
| `__xor__(self, other)` | `a ^ b` |
| `__lshift__(self, other)` | `a << b` |
| `__rshift__(self, other)` | `a >> b` |

## 43.5 容器类型相关
| 魔法函数 | 触发时机 |
| --- | --- |
| `__len__(self)` | `len(obj)` |
| `__getitem__(self, key)` | `obj[key]` |
| `__setitem__(self, key, value)` | `obj[key] = value` |
| `__delitem__(self, key)` | `del obj[key]` |
| `__contains__(self, item)` | `item in obj` |
| `__iter__(self)` | `iter(obj)`、`for x in obj` |
| `__next__(self)` | `next(obj)` |
| `__reversed__(self)` | `reversed(obj)` |
| `__missing__(self, key)` | dict 子类访问不存在的键 |

## 43.6 可调用对象
| 魔法函数 | 触发时机 |
| --- | --- |
| `__call__(self, ...)` | `obj()`，把对象当函数调用 |

```python
class Adder:
    def __init__(self, n):
        self.n = n
    def __call__(self, x):
        return x + self.n

add5 = Adder(5)
print(add5(3))   # 8
```

## 43.7 属性访问相关
| 魔法函数 | 触发时机 |
| --- | --- |
| `__getattr__(self, name)` | 访问不存在的属性时触发 |
| `__getattribute__(self, name)` | 访问任何属性时触发（更底层） |
| `__setattr__(self, name, value)` | 设置属性时触发 |
| `__delattr__(self, name)` | 删除属性时触发 |
| `__get__(self, instance, owner)` | 描述符：获取属性 |
| `__set__(self, instance, value)` | 描述符：设置属性 |
| `__delete__(self, instance)` | 描述符：删除属性 |

## 43.8 上下文管理器（with 语句）
| 魔法函数 | 触发时机 |
| --- | --- |
| `__enter__(self)` | 进入 `with` 块 |
| `__exit__(self, exc_type, exc_val, tb)` | 退出 `with` 块 |

```python
class Timer:
    def __enter__(self):
        self.start = time.time()
    def __exit__(self, *args):
        print(f"耗时: {time.time() - self.start:.2f}s")

with Timer():
    time.sleep(1)
```

## 43.9 异步相关（asyncio）
| 魔法函数 | 触发时机 |
| --- | --- |
| `__await__(self)` | `await obj` |
| `__aiter__(self)` | `async for` |
| `__anext__(self)` | `async for` 的每次迭代 |
| `__aenter__(self)` | `async with` 进入 |
| `__aexit__(self, ...)` | `async with` 退出 |

## 43.10 类相关（元类）
| 魔法函数 | 触发时机 |
| --- | --- |
| `__new__(cls, ...)` | 创建类实例 |
| `__init_subclass__(cls)` | 有子类继承时触发 |
| `__set_name__(self, owner, name)` | 描述符作为类属性被设置时触发 |
| `__prepare__(name, bases)` | 控制类命名空间创建（元类） |
| `__class_getitem__(cls, key)` | `cls[key]`，如泛型 `list[int]` |
| `__instancecheck__(self, instance)` | `isinstance(obj, cls)` |
| `__subclasscheck__(self, subclass)` | `issubclass(Sub, Base)` |

## 43.11 枚举 / 序列化相关
| 魔法函数 | 触发时机 |
| --- | --- |
| `__int__(self)` | `int(obj)` |
| `__float__(self)` | `float(obj)` |
| `__complex__(self)` | `complex(obj)` |
| `__index__(self)` | 用作索引时，`list[obj]` |
| `__round__(self, n)` | `round(obj, n)` |
| `__trunc__(self)` | `math.trunc(obj)` |
| `__floor__(self)` | `math.floor(obj)` |
| `__ceil__(self)` | `math.ceil(obj)` |

## 43.12 拷贝相关
| 魔法函数 | 触发时机 |
| --- | --- |
| `__copy__(self)` | `copy.copy(obj)` |
| `__deepcopy__(self, memo)` | `copy.deepcopy(obj)` |

## 43.13 Pickle 序列化相关
| 魔法函数 | 触发时机 |
| --- | --- |
| `__getstate__(self)` | 序列化前获取状态 |
| `__setstate__(self, state)` | 反序列化后恢复状态 |
| `__reduce__(self)` | 自定义 pickle 行为 |
| `__reduce_ex__(self, protocol)` | 指定 protocol 的 reduce |

## 43.14 最常用的 TOP 10（面试高频）
| 排名 | 魔法函数 | 用途 |
| --- | --- | --- |
| 1 | `__init__` | 对象初始化 |
| 2 | `__str__` | print / 用户可读 |
| 3 | `__repr__` | repr / 调试 |
| 4 | `__eq__` | 相等比较 |
| 5 | `__hash__` | dict/set 键 |
| 6 | `__len__` | len() |
| 7 | `__getitem__` / `__setitem__` | 索引操作 |
| 8 | `__iter__` / `__next__` | 迭代 |
| 9 | `__call__` | 对象可调用 |
| 10 | `__enter__` / `__exit__` | with 上下文管理 |

> 一句话总结：**魔法函数就是 Python 的“协议接口”，让自定义对象能自然地支持 `+`、`-`、`len()`、`print()`、`with`、`for`、`await` 等内置语法。**

# 44. MySQL 聚簇索引、非聚簇索引、二级索引、回表查询与索引下推详解

## 44.1 聚簇索引（Clustered Index）
### 44.1.1 什么是聚簇索引
**聚簇索引 = 索引和数据存放在一起**。在 InnoDB 中，表数据本身就是按照聚簇索引的键值顺序物理（逻辑）存储的。

> 通俗理解：聚簇索引的叶子节点直接存放了完整的行数据，找到了索引就找到了整行数据。

### 44.1.2 哪些列会成为聚簇索引
| 情况 | 聚簇索引 |
| --- | --- |
| 表有主键 | 主键就是聚簇索引 |
| 表没有主键 | 第一个非空唯一索引 |
| 没有主键也没有唯一索引 | InnoDB 隐式生成一个 6 字节的 `row_id` |

> 每张 InnoDB 表**有且只有一个**聚簇索引。

### 44.1.3 聚簇索引的特点
+ 叶子节点存储完整行数据
+ 查询速度快（索引即数据）
+ 插入速度依赖主键顺序（所以建议主键自增，避免页分裂）
+ 主键不要太大，否则二级索引也会变大（因为二级索引叶子存的是主键值）

## 44.2 非聚簇索引（Non-Clustered Index）
### 44.2.1 什么是非聚簇索引
**非聚簇索引 = 索引和数据分开存储**。索引的叶子节点不存放完整行数据，而是存放指向实际数据的指针。

> 关键词：**索引和数据分离**。

### 44.2.2 非聚簇索引 vs 聚簇索引
| 对比维度 | 聚簇索引 | 非聚簇索引 |
| --- | --- | --- |
| 数据存放方式 | 索引即数据，叶子节点存完整行 | 叶子节点存索引列 + 指向数据的指针 |
| 数量 | 一张表只能有 1 个 | 一张表可以有多个 |
| 查询速度 | 找到索引就找到数据，快 | 找到索引后还要根据指针去拿数据 |
| 插入更新 | 可能影响数据页顺序，开销大 | 只更新索引结构，相对独立 |
| 空间占用 | 表数据就是索引 | 额外占用索引空间 |

### 44.2.3 不同数据库/引擎的实现差异
| 数据库/引擎 | “非聚簇索引”的叶子节点存什么 |
| --- | --- |
| MySQL InnoDB | 存**主键值**（通过主键回聚簇索引查数据） |
| MySQL MyISAM | 存**行物理地址**（直接定位到磁盘行） |
| SQL Server | 可以是主键值或行地址（取决于表是否有聚集索引） |

**MyISAM 的非聚簇索引示例：**
```
数据文件（.MYD）独立存放：
  行1：id=1, name=Alice, age=20  ← 物理位置 A
  行2：id=2, name=Bob,   age=25  ← 物理位置 B

索引文件（.MYI）：
  idx_name 的叶子节点：
    Alice → 物理位置 A
    Bob   → 物理位置 B
```
查询 `name='Bob'`：
1. 在 idx_name 找到 Bob → 拿到物理位置 B
2. 直接到 .MYD 文件的 B 位置读取整行

> MyISAM 的非聚簇索引叶子节点是**物理地址指针**，不需要“回表”到另一个索引树，但还是要跨文件找数据。

## 44.3 二级索引（Secondary Index）
### 44.3.1 什么是二级索引
**二级索引 = 非聚簇索引**。也叫辅助索引。用户自己创建的普通索引、唯一索引（主键除外）都是二级索引。

> 通俗理解：二级索引的叶子节点只存**索引列 + 主键值**，不存完整行数据。

### 44.3.2 三者的关系
| 概念 | 本质 | 在 InnoDB 中 |
| --- | --- | --- |
| **聚簇索引** | 索引和数据在一起 | 主键索引 |
| **非聚簇索引** | 索引和数据分开 | 所有非主键索引 |
| **二级索引** | 非聚簇索引的另一种叫法 | 普通索引、唯一索引（除主键外） |

> **非聚簇索引 ⊇ 二级索引**：二级索引是非聚簇索引在 InnoDB 语境下的具体表现。

## 44.4 回表查询（Table Lookup）
### 44.4.1 什么是回表
**先查二级索引拿到主键值，再用主键值去聚簇索引查完整行数据**，这个过程就叫回表。

### 44.4.2 回表示例
```sql
-- 表：users(id PK, name, age)
-- 索引：idx_name(name)

SELECT * FROM users WHERE name = 'Bob';
```
执行过程：
1. 在 `idx_name` 上找到 `name='Bob'`，拿到主键 `id=2`
2. 用 `id=2` 去聚簇索引查完整行数据
3. 返回 `id, name, age`

这就是**回表**。

### 44.4.3 如何避免回表
**覆盖索引**：如果二级索引中包含了查询所需的所有字段，就不需要回表。
```sql
SELECT id, name FROM users WHERE name = 'Bob';
```
`idx_name(name)` 叶子节点已经有 `name` 和 `id`（主键），不需要回表。

## 44.5 索引下推（Index Condition Pushdown，ICP）
### 44.5.1 什么是索引下推
**把 WHERE 条件中那些可以用到索引的判断，下推到存储引擎层去做**，减少回表次数。

> MySQL 5.6 引入，默认开启。

### 44.5.2 没有 ICP 时
```sql
-- 表：users(id PK, name, age)
-- 索引：idx_name_age(name, age)

SELECT * FROM users WHERE name = 'Bob' AND age > 20;
```
没有 ICP 时：
1. 通过 `idx_name_age` 找到所有 `name='Bob'` 的记录
2. 每条都回表查完整数据
3. 在 Server 层判断 `age > 20`

如果有 10 条 `name='Bob'`，但只有 2 条 `age > 20`，那另外 8 条回表就是浪费。

### 44.5.3 有 ICP 时
1. 通过 `idx_name_age` 找到 `name='Bob'` 的记录
2. **在存储引擎层直接用索引判断 `age > 20`**
3. 只把满足条件的 2 条回表查完整数据

> 索引下推减少了回表次数，提升了查询效率。

### 44.5.4 生效条件与开关
+ 使用二级索引
+ WHERE 条件中有索引列，也有非索引列
+ MySQL 5.6+
+ 默认开启

```sql
-- 查看是否开启
SHOW VARIABLES LIKE 'optimizer_switch';

-- 关闭 ICP
SET optimizer_switch = 'index_condition_pushdown=off';
```

## 44.6 四者关系图
```
用户查询：SELECT * FROM users WHERE name = 'Bob' AND age > 20;
索引：idx_name_age(name, age)，主键 id

┌─────────────────┐
│   二级索引       │
│ idx_name_age    │
│  name  │  age   │ id  │
│  Alice │  18    │  1  │
│  Bob   │  25    │  2  │  ◄── ICP 在这里判断 age>20
│  Bob   │  30    │  5  │  ◄── ICP 在这里判断 age>20
│  Tom   │  22    │  8  │
└────────┴────────┴─────┘
         │
         ▼  满足条件的，用 id 回表
┌─────────────────┐
│   聚簇索引       │
│    （主键 id）   │
│  id  │ 完整行数据│
│   2  │ Bob,25...│
│   5  │ Bob,30...│
└──────┴───────────┘
```

## 44.7 常见面试题
### Q1：聚簇索引和二级索引的区别？
| | 聚簇索引 | 二级索引 |
| --- | --- | --- |
| 数量 | 一张表只有一个 | 可以有多个 |
| 叶子节点 | 存完整行数据 | 存索引列 + 主键值 |
| 是否回表 | 不需要 | 查全部字段时需要 |
| 大小 | 就是表本身 | 额外占用空间 |

### Q2：为什么主键推荐自增整数？
+ 自增：按顺序插入，减少页分裂
+ 整数：比较大小快，占用空间小
+ 越小：二级索引叶子节点存的主键值越小

### Q3：覆盖索引为什么快？
因为不需要回表，二级索引里已经有所有需要的数据。

### Q4：索引下推减少了什么？
减少了**回表次数**，把本应在 Server 层过滤的条件下推到存储引擎层。

> 一句话总结：**聚簇索引就是表本身，非聚簇索引/二级索引存的是索引列+主键或指针；查二级索引拿不到完整数据就要回表；索引下推能把更多过滤条件下推到引擎层，减少无效回表。**

# 45. SQL 连接方式详解：内连接、外连接、交叉连接与笛卡尔积

## 45.1 核心概念速览

| 连接类型 | 英文 | 核心作用 | 是否保留不匹配行 |
| :------- | :--- | :------- | :--------------- |
| 笛卡尔积 | Cartesian Product | 两张表所有行两两组合 | 无匹配概念 |
| 交叉连接 | CROSS JOIN | 显式的笛卡尔积 | 无匹配概念 |
| 内连接 | INNER JOIN | 只保留满足条件的行 | 不保留 |
| 左外连接 | LEFT OUTER JOIN | 保留左表全部行 | 左表保留，右表填 NULL |
| 右外连接 | RIGHT OUTER JOIN | 保留右表全部行 | 右表保留，左表填 NULL |
| 全外连接 | FULL OUTER JOIN | 保留两张表全部行 | 两边都保留，对面填 NULL |

---

## 45.2 示例数据表

**学生表 `students`：**

| student_id | name | class_id |
| :--------- | :--- | :------- |
| 1 | 张三 | 101 |
| 2 | 李四 | 102 |
| 3 | 王五 | 103 |
| 4 | 赵六 | NULL |

> 王五的班级 103 在班级表中不存在；赵六还没分班。

**班级表 `classes`：**

| class_id | class_name |
| :------- | :--------- |
| 101 | 一班 |
| 102 | 二班 |
| 104 | 四班 |

> 四班目前没有学生。

---

## 45.3 笛卡尔积（Cartesian Product）

### 定义
把表 A 的每一行与表 B 的每一行进行全部可能的组合。

**结果行数 = A 行数 × B 行数**

### SQL 写法
```sql
SELECT * FROM students, classes;
```

### 执行结果（4 × 3 = 12 行）

| student_id | name | students.class_id | classes.class_id | class_name |
| :--------- | :--- | :---------------- | :--------------- | :--------- |
| 1 | 张三 | 101 | 101 | 一班 |
| 1 | 张三 | 101 | 102 | 二班 |
| 1 | 张三 | 101 | 104 | 四班 |
| 2 | 李四 | 102 | 101 | 一班 |
| 2 | 李四 | 102 | 102 | 二班 |
| 2 | 李四 | 102 | 104 | 四班 |
| 3 | 王五 | 103 | 101 | 一班 |
| 3 | 王五 | 103 | 102 | 二班 |
| 3 | 王五 | 103 | 104 | 四班 |
| 4 | 赵六 | NULL | 101 | 一班 |
| 4 | 赵六 | NULL | 102 | 二班 |
| 4 | 赵六 | NULL | 104 | 四班 |

### 执行原理
数据库不做任何关联判断，直接把两张表的数据暴力组合。

### 使用场景
- 生成测试数据
- 生成所有组合，如「商品 × 尺码 × 颜色」生成 SKU
- 业务查询中通常要避免无条件的笛卡尔积，防止数据爆炸

---

## 45.4 交叉连接（CROSS JOIN）

交叉连接就是**显式写出来的笛卡尔积**，结果与隐式笛卡尔积完全相同。

### SQL 写法
```sql
SELECT * FROM students CROSS JOIN classes;
```

### 关键规则
- `CROSS JOIN` 后面**不能写 `ON` 条件**
- 因为它本质上就是笛卡尔积，没有匹配逻辑

错误写法：
```sql
SELECT *
FROM students
CROSS JOIN classes ON students.class_id = classes.class_id;  -- 语法错误
```

### 可以加 WHERE 过滤吗？
可以，但语义变了：
```sql
SELECT *
FROM students
CROSS JOIN classes
WHERE students.class_id = classes.class_id;
```
结果与 INNER JOIN 相同，但执行过程是「先全组合，再过滤」，效率通常不如 INNER JOIN。

### 交叉连接 vs 笛卡尔积

| 对比项 | 隐式笛卡尔积 | 显式 CROSS JOIN |
| :----- | :----------- | :--------------- |
| 写法 | `FROM a, b` | `FROM a CROSS JOIN b` |
| 结果 | 相同 | 相同 |
| 可读性 | 容易误写漏条件 | 明确表示要全组合 |
| 推荐度 | 一般 | 更清晰 |

---

## 45.5 内连接（INNER JOIN）

### 定义
只保留**两个表中满足连接条件**的行，不匹配的行全部丢弃。

### SQL 写法
```sql
SELECT s.*, c.class_name
FROM students s
INNER JOIN classes c ON s.class_id = c.class_id;
```

`INNER` 可以省略，写成：
```sql
SELECT s.*, c.class_name
FROM students s
JOIN classes c ON s.class_id = c.class_id;
```

### 执行结果

| student_id | name | class_id | class_name |
| :--------- | :--- | :------- | :--------- |
| 1 | 张三 | 101 | 一班 |
| 2 | 李四 | 102 | 二班 |

### 执行原理
1. 先做笛卡尔积，生成 12 行
2. 按 `ON s.class_id = c.class_id` 过滤
3. 只保留 class_id 相等的行

### 被过滤掉的行

| 被过滤原因 | 行 |
| :--------- | :--- |
| 王五的 class_id=103 在班级表中不存在 | 王五 与 所有班级 的组合 |
| 赵六的 class_id 为 NULL，无法匹配 | 赵六 与 所有班级 的组合 |
| 四班 class_id=104 没有学生 | 所有学生 与 四班 的组合 |

### 使用场景
- 最常见的关联查询
- 只关心有完整关联关系的数据
- 例如：查询有班级的学生

---

## 45.6 外连接（OUTER JOIN）

外连接的核心：**保留一侧或两侧的所有行，没有匹配到的列用 NULL 填充。**

### 45.6.1 左外连接（LEFT JOIN / LEFT OUTER JOIN）

#### 定义
保留**左表（students）的所有行**，右表没有匹配上的列填 NULL。

#### SQL 写法
```sql
SELECT s.*, c.class_name
FROM students s
LEFT JOIN classes c ON s.class_id = c.class_id;
```

#### 执行结果

| student_id | name | class_id | class_name |
| :--------- | :--- | :------- | :--------- |
| 1 | 张三 | 101 | 一班 |
| 2 | 李四 | 102 | 二班 |
| 3 | 王五 | 103 | NULL |
| 4 | 赵六 | NULL | NULL |

#### 执行原理
1. 先做 INNER JOIN，得到张三、李四
2. 检查左表（students）中还有哪些行没出现
3. 把王五、赵六补回来
4. 右表（classes）对应的列填 NULL

#### 使用场景
- 查询所有学生及其班级，包括未分班的学生
- 查找缺失关联数据，如「哪些学生没有班级」

```sql
SELECT s.*
FROM students s
LEFT JOIN classes c ON s.class_id = c.class_id
WHERE c.class_id IS NULL;
```
结果：王五、赵六。

---

### 45.6.2 右外连接（RIGHT JOIN / RIGHT OUTER JOIN）

#### 定义
保留**右表（classes）的所有行**，左表没有匹配上的列填 NULL。

#### SQL 写法
```sql
SELECT s.*, c.class_name
FROM students s
RIGHT JOIN classes c ON s.class_id = c.class_id;
```

#### 执行结果

| student_id | name | class_id | class_name |
| :--------- | :--- | :------- | :--------- |
| 1 | 张三 | 101 | 一班 |
| 2 | 李四 | 102 | 二班 |
| NULL | NULL | NULL | 四班 |

#### 执行原理
1. 先做 INNER JOIN，得到张三、李四
2. 检查右表（classes）中还有哪些行没出现
3. 把四班补回来
4. 左表（students）对应的列填 NULL

#### 使用场景
- 实际开发中很少使用
- 因为交换表顺序后可以用 LEFT JOIN 表达，可读性更好

---

### 45.6.3 全外连接（FULL JOIN / FULL OUTER JOIN）

#### 定义
保留**两个表的所有行**，任何一边没有匹配上的，对面列填 NULL。

#### SQL 写法
```sql
SELECT s.*, c.class_name
FROM students s
FULL JOIN classes c ON s.class_id = c.class_id;
```

#### 执行结果

| student_id | name | class_id | class_name |
| :--------- | :--- | :------- | :--------- |
| 1 | 张三 | 101 | 一班 |
| 2 | 李四 | 102 | 二班 |
| 3 | 王五 | 103 | NULL |
| 4 | 赵六 | NULL | NULL |
| NULL | NULL | NULL | 四班 |

#### 执行原理
1. 先做 INNER JOIN
2. 把左表没匹配上的行补回来
3. 把右表没匹配上的行补回来

#### 注意
MySQL **不支持** `FULL JOIN`，需要用 `UNION` 模拟：
```sql
SELECT s.*, c.class_name
FROM students s
LEFT JOIN classes c ON s.class_id = c.class_id

UNION

SELECT s.*, c.class_name
FROM students s
RIGHT JOIN classes c ON s.class_id = c.class_id;
```

---

## 45.7 韦恩图理解

```
        ┌─────────────────────┐
        │     students        │
        │   ┌───────────┐     │
        │   │  INNER    │     │        classes
        │   │  JOIN     │     │    ┌───────────┐
        │   │  张三     │     │    │   四班    │
        │   │  李四     │     │    └───────────┘
        │   └───────────┘     │
        │   王五     赵六     │
        └─────────────────────┘
```

| 连接类型 | 对应区域 |
| :------- | :------- |
| INNER JOIN | 两个圆重叠部分 |
| LEFT JOIN | 整个左圆 |
| RIGHT JOIN | 整个右圆 |
| FULL JOIN | 两个圆全部 |
| CROSS JOIN | 左圆每一点与右圆每一点连线 |

---

## 45.8 执行顺序的本质

数据库执行 JOIN 的底层过程：

1. **生成笛卡尔积**（所有行组合）
2. **按 ON 条件过滤**
3. 如果是 OUTER JOIN，把被过滤掉的**保留侧**行补回来，缺失侧填 NULL

| 连接类型 | 本质 |
| :------- | :--- |
| CROSS JOIN | 笛卡尔积，不过滤 |
| INNER JOIN | 笛卡尔积 + 按 ON 过滤 |
| LEFT JOIN | 笛卡尔积 + 过滤 + 补回左表 + 右表填 NULL |
| RIGHT JOIN | 笛卡尔积 + 过滤 + 补回右表 + 左表填 NULL |
| FULL JOIN | 笛卡尔积 + 过滤 + 两边都补回 + 对面填 NULL |

---

## 45.9 常见误区

### 误区 1：ON 条件可以写在 WHERE 里？

**对于 INNER JOIN 通常一样，但对于 OUTER JOIN 完全不同。**

错误示例：
```sql
SELECT *
FROM students s
LEFT JOIN classes c ON s.class_id = c.class_id
WHERE c.class_name = '一班';
```

这个查询会先 LEFT JOIN 保留所有学生，然后用 WHERE 过滤掉 class_name 不是『一班』的行。最终只保留张三，王五、赵六会丢失。

**正确做法**是把过滤条件放到 ON 里：
```sql
SELECT *
FROM students s
LEFT JOIN classes c ON s.class_id = c.class_id AND c.class_name = '一班';
```
这样所有学生都会保留，只是没匹配上的 class_name 为 NULL。

### 误区 2：NULL 能匹配上吗？

不能。任何值与 NULL 比较结果都是 UNKNOWN，不会匹配。

所以赵六的 `class_id = NULL` 永远不会匹配任何班级。

### 误区 3：LEFT JOIN 一定比 INNER JOIN 慢？

不一定。数据库优化器可能会选择相同的执行计划。但如果 LEFT JOIN 导致结果集显著变大，确实可能更慢。

### 误区 4：CROSS JOIN 需要 ON 条件？

不需要，也不能写。CROSS JOIN 就是笛卡尔积。

---

## 45.10 使用场景对照表

| 连接类型 | 典型使用场景 |
| :------- | :----------- |
| CROSS JOIN | 生成 SKU、测试数据、所有组合场景 |
| INNER JOIN | 最常见的关联查询，只查有匹配的数据 |
| LEFT JOIN | 查主表全部数据，附带关联表信息，允许缺失 |
| RIGHT JOIN | 很少用，可用 LEFT JOIN 交换表顺序替代 |
| FULL JOIN | 对比两表差异、合并数据、找出两边各自缺失的数据 |

---

## 45.11 快速记忆口诀

> **内连接取交集，外连接留一边，全外两边都留，交叉全配对。**

| 口诀 | 含义 |
| :--- | :--- |
| 内连接取交集 | INNER JOIN 只保留两边都匹配的行 |
| 外连接留一边 | LEFT/RIGHT JOIN 保留一边所有行 |
| 全外两边都留 | FULL JOIN 保留两边所有行 |
| 交叉全配对 | CROSS JOIN 所有行两两组合 |

> 一句话总结：**JOIN 的本质是笛卡尔积 + 过滤；INNER 只留匹配，OUTER 还补 NULL，CROSS 什么都不做。**

#  一些代码题
###  时间相加
````python
from datetime import datetime, timedelta


def add_days(date_str, n):
    # 把字符串解析成 datetime 对象
    dt = datetime.strptime(date_str, "%Y%m%d")
    # 加上 N 天
    new_dt = dt + timedelta(days=n)
    # 格式化成 YYYYMMDD
    return new_dt.strftime("%Y%m%d")


print(add_days("20190530", 2))  # 输出 20190601
关键点：

strptime 按 %Y%m%d 解析字符串
timedelta(days=n) 直接做日期加减，会自动处理跨月、跨年的情况
strftime 再格式化回去
如果是 N 为负数，就是算 N 天前，timedelta 也能直接处理。
````

###  写一个函数，接收整数参数 n，返回一个函数。函数的功能是把函数的参数和 n 相乘并把结果返回。
````python
def make_multiplier(n):
    def multiply(x):
        return x * n
    return multiply


double = make_multiplier(2)
triple = make_multiplier(3)

print(double(5))  # 10
print(triple(5))  # 15

核心点：
内部函数 multiply 记住了外部函数 make_multiplier 传入的 n，即使 make_multiplier 已经执行完，n 依然被内部函数持有。这就是闭包。
````

或者用 lambda 更简洁：

````python
def make_multiplier(n):
    return lambda x: x * n
x 是返回的那个函数的入参，调用的时候传进去的。
````
看完整流程：

````python
def make_multiplier(n):
    return lambda x: x * n

double = make_multiplier(2)   # n = 2，返回一个等待 x 的函数
result = double(5)            # 这里 x = 5
print(result)                 # 输出 10
# 1. n 是在定义 lambda 的时候从外层 make_multiplier 捕获的，叫闭包变量。
# 2. x 是普通参数，调用 double(5) 时才确定。
也可以这样理解：


double = lambda x: x * 2
x 不固定，调用时传什么就是什么。
````

### 单例模式
Python 单例模式的常见实现方法：

**1. 模块级别（最 Pythonic）**

Python 模块天然是单例，只会被导入一次。
````python
# singleton.py
class Singleton:
    def __init__(self):
        self.value = 0

instance = Singleton()

# 使用时
from singleton import instance
````

**2. 装饰器实现**
````python
def singleton(cls):
    instances = {}
    def get_instance(*args, **kwargs):
        if cls not in instances:
            instances[cls] = cls(*args, **kwargs)
        return instances[cls]
    return get_instance

@singleton
class Database:
    def __init__(self):
        self.connection = "connected"
````

**3. `__new__` 方法实现**
````python
class Singleton:
    _instance = None
    def __new__(cls, *args, **kwargs):
        if cls._instance is None:
            cls._instance = super().__new__(cls)
        return cls._instance

    def __init__(self):
        # 注意：__init__ 每次实例化都会被调用
        if not hasattr(self, '_initialized'):
            self.value = 0
            self._initialized = True
````

**4. 元类（Metaclass）实现（推荐用于复杂场景）**
````python
class SingletonMeta(type):
    _instances = {}
    def __call__(cls, *args, **kwargs):
        if cls not in cls._instances:
            cls._instances[cls] = super().__call__(*args, **kwargs)
        return cls._instances[cls]

class Logger(metaclass=SingletonMeta):
    def __init__(self):
        self.logs = []
````

**5. 线程安全版本**
````python
import threading

class Singleton:
    _instance = None
    _lock = threading.Lock()

    def __new__(cls):
        if cls._instance is None:
            with cls._lock:
                # 双重检查
                if cls._instance is None:
                    cls._instance = super().__new__(cls)
        return cls._instance
````

**为什么需要线程安全版本？**

因为前面的普通版本在**多线程并发懒加载**时会失效，可能创建出**多个实例**，破坏单例。问题出在「检查 → 创建」不是原子操作：
````python
def __new__(cls):
    if cls._instance is None:                # ← 步骤1：检查
        cls._instance = super().__new__(cls) # ← 步骤2：创建
    return cls._instance
````
多线程会这样出事：
````
时刻   线程A                        线程B
 t1    if _instance is None → True
 t2                                 if _instance is None → True  ← A还没来得及创建
 t3    创建实例1，赋值给 _instance
 t4                                 创建实例2，覆盖 _instance     ← 又创建了一个！
````
结果创建了 2 个实例，单例失败。

**双重检查锁定（Double-Checked Locking）三个要点：**

+ **外层 `if`**：实例已创建好后，绝大多数调用直接返回，不用抢锁（加锁很慢，避免每次都锁）。
+ **`with cls._lock`**：保证「检查+创建」这段临界区同一时刻只有一个线程能执行。
+ **内层 `if`（第二次检查）**：最关键。A、B 可能都通过外层检查、都在锁门口排队。A 拿锁创建完释放后，B 进来若不再查一次，就会又创建一个覆盖掉。第二次检查让 B 发现“已经有了”直接跳过。

**什么时候真的需要？**

| 场景 | 是否需要线程安全版 |
| --- | --- |
| 单线程程序 / Web 请求各自独立 | 不需要，普通版即可 |
| 多线程且**在运行中懒加载**单例 | **需要**，否则可能创建多个 |
| 程序启动时就初始化好（模块级） | 不需要——**模块导入由解释器加锁保证只执行一次，天然线程安全** |

> 一句话：线程安全版是为了解决**多线程懒加载**时「检查-创建」竞态导致的重复实例化。而用**模块级单例**（推荐），Python 导入机制已保证线程安全，根本不用自己加锁。

**对比与选择**

| 方法 | 优点 | 缺点 |
| --- | --- | --- |
| 模块 | 简单、原生支持 | 无法延迟加载 |
| 装饰器 | 使用方便 | 继承处理复杂 |
| `__new__` | 直观 | `__init__` 重复调用 |
| 元类 | 干净、可继承 | 理解门槛稍高 |
| 线程安全 | 多线程可靠 | 性能略有开销 |
----
> 推荐优先级：**模块 > 元类 > 装饰器 > `__new__`**。多数场景下，模块级单例最简洁可靠。

